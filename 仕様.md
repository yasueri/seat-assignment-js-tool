# 仕様書 — CSV入力チェックと座席配置

座席配置ツール **ver0.5.8** の現在の仕様。**現在どうなっているかだけ**を書く。
「いつ・なぜ変えたか」は `履歴.md`、「これから何をするか」は `課題.md` にある。

> **改修・検証を依頼されたら、まずこのファイルと `課題.md` を読むこと。**
> 章番号（§0〜§9）は文中の相互参照に使っているため、**変更しない。**
> 仕様を変えたときは、このファイルを**上書き**し、変更の経緯を `履歴.md` の先頭に追記する。

〈verX.Y.Zで追加／変更〉という注記は、**まだ枯れていない箇所の目印**として残してある。

---

## 0. 決定サマリ（これだけで全体が分かる）

### 中断する条件（すべて「選択日に出勤している人」のみ対象）

| # | 条件 | 状態 |
|---|---|---|
| A-1 | 必要な列のヘッダーが不一致（右への列追加は可） | ver0.5.7で中断へ格上げ |
| A-2 | 文字化けしている（`U+FFFD`） | ver0.5.7で実装 |
| B-1 | secret.csv：同じ人が同じ種別に複数行 | 実装済み（ver0.5.7で出勤者に限定） |
| B-2 | ojt.csv：同じ教官が複数行 | 実装済み（ver0.5.7で出勤者に限定） |
| B-3 | ojt.csv：同じOJT対象者が複数の教官に | 実装済み（ver0.5.7で出勤者に限定） |
| B-4 | 1人に、座る席を指名するルールが2つ以上 | ver0.5.7で実装 |
| B-5 | 固定席・要サポートの座席が、同じ人の禁止席にも入っている | ver0.5.7で実装 |
| B-6 | 教官・OJT対象者に優先フラグ | ver0.5.7で実装 |
| B-7 | 禁止席の指定で座れる席がゼロ | ver0.5.7で実装 |
| B-8 | 選択日の行が読み飛ばされた（月間シフトCSV） | ver0.5.7で中断へ格上げ |
| B-9 | 優先フラグだけが効いてしまう行 | ver0.5.7で中断へ格上げ／ver0.5.7.1で対象を拡張 |
| B-10 | ojt.csv：同じ人が「教官」と「OJT対象者」の両方に書かれている | ver0.5.7.4で実装 |
| B-11 | ojt.csv の教官が rookie.csv にも登録されている | ver0.5.7.4で実装 |

### 優先フラグが付いている行の早見表（仕様が衝突しやすいのはここだけ）

| 誰に × 種別 | 扱い |
|---|---|
| 教官・OJT対象者 × 種別を問わず | **中断**（B-6。ただし教官・OJTの処理が動く日に限る。§4参照） |
| 新人 × 固定席／要サポート | **中断**（優先フラグがあっても許容しない・B-4） |
| 新人 × 禁止席／隣接禁止 | 許容（新人固定席は効かなくなるが、止めるだけの理由がない） |
| 新人 × 種別なし | **中断**（B-9） |
| その他の人 × 固定席／禁止席／要サポート／隣接禁止 | 許容（従来どおり） |
| その他の人 × 種別なし | **中断**（B-9） |
| 誰でも × 種別は書いたが、その行の指定が読み飛ばされた | **中断**（B-9。ver0.5.7.1で追加。例:「固定席,山田 太郎,,,1」） |
| 誰でも × 優先フラグの値が数値でない | その優先フラグを無視（弾く）。B-9の対象外 |

### 用語（マニュアル・メッセージ・ソースのコメントで統一する）

- **座る席を指名するルール** … 固定席／要サポート／新人（rookie.csv）／OJT（ojt.csv）。
  **1人にどれか1つだけ。** 2つ以上あると、後から処理されるほうが黙って無視される
- **座らせない席を外すルール** … 禁止席／隣接禁止。上のどれかと一緒に使える
- **隣接禁止の「隣」** … 上下・左右・斜めのすべて。**同じ座席（1人目と2人目）も含む**
  〈ver0.5.7.4で明文化〉。隣の席がだめで同じ机ならよい、ということはないため
- **優先フラグ** … 席は決めない。処理の順番を早めるだけ

### 備忘：新人に座席を指定したい場合

**優先フラグで代用させない。**
将来、新人の座席を指定したいという要請が出たら、ojt.csv のように対象座席を
書ける形式へ変更して対応する。
マニュアルには「**新人の座席を（ojt.csvのように）指定したい場合は、
開発者に連絡してください**」と記載済み（7章の新人カード・優先フラグカード、12章）。


---

## 1. 基本方針

### 何を基準に分けるか

**その行が読み飛ばされたことに、完成した座席表を見て気づけるか。**

- 気づけない → **配置を止める**（黙って間違った座席表が印刷されるため）
- 気づける／影響がその行だけ → **メッセージ欄に出して続行**

「明らかな間違いかどうか」で分けると線引きが人によってブレるため、この基準で統一する。

### 止めてよい条件

**直し方が一つに決まるものだけを止める。** 判断が必要なもの、逃げ道が必要なものは止めない。

現在の中断条件は、いずれもCSV間の矛盾であり、どちらか一方を消せば必ず解決する。

B-9（優先フラグ単独）だけは矛盾ではなく**記入漏れ**だが、「優先フラグを消す」という
一意の直し方があるため同じ扱いとする。

**逆に、直し方が一つに決まらないものは止めてはいけない。**
rookie.csv の人が役席・GLで出勤するケース（§5）がその実例で、ver0.5.6 で中断にしたが
撤回した。同じ人がOP勤務の日は新人ルールを効かせたいのに、役席・GL勤務の日だけ止まる。
すると rookie.csv の行を消すしかなくなり、OP勤務の日の新人ルールまで失われる。

---

## 2. 中断の共通仕様

### 判定の対象範囲

**選択日に出勤している人に関するものだけを中断対象とする。**

出勤していない人の設定矛盾は、その日の座席表に影響しない。全員を対象にすると、
休職者の設定が残っているだけで毎日止まることになる。

判定の基準になる氏名の集合は、**その日の月間シフトCSVの行に載っている人（`dayRows`）全員。**
早番・遅番エリアや夜勤GL枠へ回る役席・GLも含む。

> ※ **「座席1〜15に並ぶ人」まで絞らない。**
> 座る席を指名するルール・禁止席・優先フラグはいずれも座席1〜15に対するルールなので、
> 「枠側へ回る役席・GLには効かないから、その日は止めなくてよい」と一度は考えたが撤回した。
> バッジはCSVに書かれているかどうかだけで決まり（`deriveBadgeFields`）、枠へ回った人にも
> 付けているため（`reapplyLeaderBadges`）、**手動で座席へ動かした時点で矛盾したバッジが
> 説明なしに表示されてしまう。** 詳細は§7。
>
> ※ **例外はB-6・B-11の教官のみ。** 担当するOJT対象者がその日1人も出勤していなければ、
> 教官は通常のスタッフとして配置されるため矛盾にならない（§4の「教官×固定席」と同じ扱い）。
> この2件だけは「教官本人が座席側にいて、かつその教官の担当するOJT対象者の
> 少なくとも1名も座席側にいる日」に限って中断する〈B-11はver0.5.7.4で追加〉。
>
> ※ **B-8で日付そのものが読めなかった行は、選択日を問わず中断する。**
> どの日の勤務か判別できないため、選択日の行かどうかを判定できない。
> 「出勤者のみ対象」から外れる唯一の例外。VBA出力のため日付が読めなくなる頻度は極めて低い。

既存の中断条件3件（B-1〜B-3）も ver0.5.7 でこの範囲に合わせた。出勤の有無にかかわらず
止まっていた挙動が変わっている。ojt.csv の教官重複・担当重複は解析時（`parseOjtRows`）に
エラーを積んでいたため、**メッセージの生成を呼び出し側（`ui.js` の自動配置実行時）へ移した。**
`parseOjtRows` は `duplicateMentors` / `duplicateTrainees` として関係者の氏名だけを返す。

### 止め方

- 行番号つきで**メッセージ欄**に表示し、配置を実行しない
- ダイアログは「**実行できていないことに気づかせる**」ために1回だけ出す
  （alertは本文をコピーできず行数が多いと読み切れないため、詳細はメッセージ欄で読ませる）
- **中断理由は一度に全部出す。** 最初の1件で打ち切ると
  「直す→再実行→次のエラー」の繰り返しになる
- 中断理由はメッセージ欄の**先頭**に置く（読み飛ばし系の警告に埋もれさせないため）

### 併せて必要な修正（実装済み）

- **色の意味を統一した。** ver0.5.6 までは `error`（赤）に「配置が止まっている」
  「座席表はできたが一部の人を置けなかった」「手動で人を動かした後のルール違反」が
  混在しており、「赤が出ているのに座席表が完成している」状態が生まれていた。
  レベルを1つ増やして4段階に整理した。

  | レベル | 色 | 意味 |
  |---|---|---|
  | `error` | 赤 | 座席表ができていない（中断） |
  | `violation`（新設） | オレンジ | 座席表はできたが、置けない人・ルール違反がある |
  | `warn` | 黄 | 読み飛ばし・注意喚起 |
  | `info` | 青 | お知らせ |

  `violation` へ移したもの：`algorithm.js` の「◯◯さんを配置できません」系8か所と、
  `ui.js` の手動変更後の違反チェック表示。

  現場への説明は2段階でよい。
  **「赤が出たら座席表はできていない。それ以外は座席表を見ながら確認する」**
  index.html のメッセージ欄に4段階の凡例を1行入れ、マニュアル12章に表を置いた。

- **中断時は前回の座席表を画面に残さない**（`clearBoard`）。あわせて `hasRunOnce` を
  false に戻すため、**印刷・保存も止まる。** 印刷ボタンは座席表が無い状態で押すと
  その旨を伝えて何もしない。

- **やり直しの確認（「手動で調整した内容は失われます」）は中断判定より前に置く**
  〈ver0.5.7.1で修正〉。後ろに置いていたため、中断が起きるときだけ確認なしに
  盤面が消えていた。誤って実行ボタンを押したときの取り消し手段が、中断の有無で
  変わってはいけない。キャンセルした場合は中断メッセージも出ないが、実行を
  やめた以上それでよい。

- 中断理由は複数行になるため、`.log-line` に `white-space:pre-wrap` を追加した。

---

## 3. 【A】読み込み時に中断（ダイアログ）

ファイル単位で成立しないもの。実行ボタンを押す前に気づかせる。
実装は `csv.js` の `preflightCsv(fileKey, text)`。`ui.js` の `loadFileInto` が
読み込んだ直後に呼び、不合格ならファイルを採用しない。

| # | 条件 | 理由 |
|---|---|---|
| A-1 | **必要な列**のヘッダーが想定と異なる（右への列追加は除く） | 列の位置でデータを読んでいるため、列順が違うと全行が別の意味で読まれる。最も危険な「黙って間違う」ケース |
| A-2 | 文字化けしている | 氏名が全滅するため実質ファイル不良 |

**不合格のときは、前に読めていた内容も残さない。** どちらが使われているのか
分からなくなるため、`rawText[key] = null` にして「読み込めませんでした」と表示する。
月間シフトCSVの場合は日付セレクタも空に戻す。

**ただし画面の座席表は消さない**〈ver0.5.7.1で明文化〉。実行時の中断で盤面を消すのは、
利用者が「作り直す」と言って実行ボタンを押したため。読み込み時の不合格は、まだ
作り直しを頼まれていない。表示中の座席表は正しい内容のままで、日付ラベルも一致して
いるので、そのまま印刷しても間違いは起きない。消す設計にすると、翌月分のCSVを
読み込もうとして間違えただけでその日の座席表が失われ、実害はこちらのほうが大きい。

### A-1 の判定範囲

各CSVで**必要な列（左からn列）のラベルが、順番どおりに一致するか**だけを見る。
**完全一致ではなく前方一致。** n列より右に列が増えていることは許容し、その内容は読まない。

| ヘッダーの状態 | 扱い |
|---|---|
| 必要な列より**右**に列が増えている | 許容（読み飛ばす） |
| 必要な列の並べ替え・ラベル変更・列不足 | 中断 |
| 必要な列の**間**に列を挿入 | 中断（以降が全部ずれるため前方一致で自動的に拾える） |

例：secret.csv は1〜5列目（種別・対象1・対象2・対象座席・優先フラグ）が一致していればよく、
6列目以降に利用者説明用のメモ列があってもよい。6列目が空欄で7列目にメモ列でも同じ。

**通ったときはメッセージを出さない。** メモ列を常設する運用だと毎回出て邪魔になるため。
代わりにマニュアルの9章・10章に「必要な列より右に、自分用の列を足してもよい
（ツールは読みません）」と記載した。

必要な列の定義は `csv.js` の `HEADER_SPECS` に1か所だけ持ち、
4つの解析関数はこれを参照する（解析関数側の `checkHeader` 呼び出しは廃止した）。

### メモ列にカンマ・改行を書いても壊れない（対応済み）

**CSV解析はすでに引用符（RFC 4180）に対応している。**
`csv.js` のパーサーは1文字ずつ走査する実装で、BOM除去・引用符・`""` のエスケープ・
引用符内の改行のすべてを扱える。4つの解析関数はいずれもこれを通っている。

- メモ列にカンマ … 列が増えるだけで無害（6列目以降は読まない）
- メモ列にセル内改行 … Excelが `"` で囲んで保存し、パーサーが正しく1行として読む

したがってマニュアルに「メモ列にカンマ・改行は書かない」と書く必要はない。

### A-1・A-2 の実装位置

- **secret.csv / ojt.csv / rookie.csv は、実行ボタンを押した時に初めて解析される。**
  読み込み時点では本文を変数に持つだけ。そのため `preflightCsv` は解析関数の外に置き、
  `loadFileInto` から呼んでいる（解析を待たずにヘッダーと文字化けだけを見る）
- 月間シフトCSVは読み込み時に解析するが、**行ごとの警告が画面に出るのは実行時**。
  ただし**配置に使える行が0件のときは、A-1・A-2と同じく読み込みを不合格にする**
  〈ver0.5.8で変更〉。中身が1行も使えないファイルは「そのファイルは使えない」という点で
  ヘッダー不一致・文字化けと同じため、扱いをそろえた（`rejectFile` を通す＝状態表示は
  「読み込めませんでした」、赤い本文＋行ごとの警告、ダイアログ1回、メッセージ欄へスクロール）。
  ver0.5.7.5までは赤い本文だけを出しながら「読み込み済み」と表示し、直後に
  「月間シフトCSVを読み込みました。」という青い行まで並べていた。ダイアログもスクロールも
  出ないため（`endLoadBatch` は失敗0件だと出さない）、肝心のメッセージが画面の下に埋もれていた
- A-2 は、ファイル読み込みが常にUTF-8として解釈する仕様のため、
  **読み込んだ本文に `U+FFFD`（`�`）が1文字でも含まれていたら中断**でよい。
  文字化けしているとヘッダーも必ず不一致になるため、**両方は出さない**（A-2を優先）

### 読み込み結果はまとめて1回だけ表示する

〈ver0.5.7.1で追加〉ver0.5.7 まではファイル1つごとにメッセージ欄を書き換えていたため、
まとめてドラッグ&ドロップすると次の2つが起きていた。

- 先に出したエラーが、後続のファイルの表示（「バッジ表示を更新しました」など）で消える
- ダイアログがファイルの数だけ続けて出る

読み込み処理の間はメッセージをためておき、最後に**メッセージ欄1回・ダイアログ1回**で出す
（`ui.js` の `beginLoadBatch` / `emitMessages` / `endLoadBatch`）。
メッセージ欄の並びは**赤（読み込めなかったもの）を先頭**にし、実行時の中断と揃える。

「ファイル名から種類を判別できませんでした」の別ダイアログも、この1回に合流させた。
**ボタンで1つずつ読み込む場合も同じ経路を通す**（挙動を分けない）。

なお、ファイルそのものを開けなかった場合（`loadFileInto` の catch）は ver0.5.7 まで
状態表示が変わるだけで何も出ていなかったため、メッセージとダイアログを出すようにした。

### 中断ダイアログの文言（読み込み時）

**件数とファイル名だけを伝え、詳細はメッセージ欄で読ませる**〈ver0.5.7.1で変更〉。
ver0.5.7 まではメッセージ本文をそのままダイアログに出していたが、A-1の本文は
「想定」「実際」の列比較を含むため1ファイルでも6〜8行あり、複数ファイルだと
読み切れない。**実行時の中断（B）のダイアログと作りを揃える**ことで、現場への説明が
「赤が出たらメッセージ欄を読む」の1つで済む。件数による出し分けはしない。

> 3個のファイルを読み込めませんでした（月間シフトCSV / secret.csv / ojt.csv）。
>
> 内容は画面の「2. メッセージ」欄（赤色）に表示しています。修正してから、
> もう一度読み込んでください。

1ファイルのときは「secret.csv を読み込めませんでした。」と名前だけを出す。

### A-1 の文言

以下はメッセージ欄に出す本文の話（ダイアログには出さない）。
月間シフトCSVはVBA出力のためヘッダーは常に一定。不一致が出たら原因は
**ファイルの取り違え**か**VBAの出力仕様の変更**のどちらかであり、日常運用ではまず出ない。
secret.csv・ojt.csv・rookie.csvは手で管理するため、**必要な列を消した・並べ替えた・
間に列を挿入した**という3つ目の原因もある。ダイアログの文言では、この2種類を出し分けている。

---

## 4. 【B】実行時に中断（メッセージ欄＋ダイアログ1回）

実装は `ui.js` の `collectBlockingProblems`。中断理由の文字列を配列で返し、
1件以上あれば `clearBoard` → メッセージ欄 → ダイアログ1回の順で処理して実行しない。

### B-1〜B-3 既存の条件（判定範囲のみ変更）

| # | 条件 | 出勤者の判定 |
|---|---|---|
| B-1 | secret.csv：同じ人が同じ種別（固定席／禁止席／要サポート）に複数行ある | 本人が出勤している |

| B-2 | ojt.csv：同じ教官が複数行にある | 教官本人、またはその行のOJT対象者の誰かが出勤している |
| B-3 | ojt.csv：同じOJT対象者が複数の教官に紐づいている | OJT対象者本人が出勤している |

B-2で教官本人だけでなくOJT対象者も見るのは、2行目以降が読み飛ばされると
**そこに書かれたOJT対象者が担当教官を失う**ため。教官が休みでも座席表に影響する。

B-1で「複数行」と数えるのは、**実際に座席の指定を1件以上作れた行だけ**〈ver0.5.8で変更〉。
`固定席,甲,,99番,` のように座席番号がすべて無効で1件も指定を作れなかった行は、
効いていないので数えない（その行には黄色の警告が別に出る）。効いていない行を理由に
配置を止めるのは、B-1の目的（後の行が黙って無視される）から外れる。

B-2・B-3の重複判定と、メッセージに出す氏名は、**どちらも `nameKey`（空白を除いた氏名）を
キーにそろえてある**〈ver0.5.8で修正〉。以前は判定が `nameKey`・集計が表示用の氏名という
取り違えがあり、「山田太郎」と「山田 太郎」のように表記だけ違う2行では、メッセージに
2行目の表記しか出ないことがあった。表示には、そのキーで**最初に出てきた表記**を使う。

### B-4 座る席を指名するルールが1人に2つ以上当たっている

座る席を指名するルールは次の4つで、この優先順に処理される。

**新人固定席 → 教官・OJT → 固定席 → 要サポート**

同じ人にこのうち**2つ以上**が当たっている場合、**優先順位の後ろにあるルールは
メッセージも出さずに消える。**

対象となる組み合わせ：

| 組み合わせ | 扱い |
|---|---|
| 新人 × OJT対象者 | 中断 |
| 新人 × 固定席 | 中断（**優先フラグが付いていても中断する**） |
| 新人 × 要サポート | 中断（**優先フラグが付いていても中断する**） |
| OJT対象者 × 固定席 | 中断 |
| OJT対象者 × 要サポート | 中断 |
| 固定席 × 要サポート | 中断 |

**現場への説明：「座る席を指名するルールは、1人に1つだけ」**（例外なし）

禁止席・隣接禁止は「座らせない席を外すルール」なので、上の4つのどれかと一緒に使える。
この2つはB-4の対象に含めない。

#### 対象に含めないもの

※ **優先フラグ。** 座席を決めるルールではなく、処理の順番を早めるだけのものであるため。
優先フラグを付けても上記の組み合わせは中断する。単独で書いた場合は**B-9**で中断。

※ **教官 × 固定席／要サポート。** OJT対象者がその日1人も出勤していなければ、
教官は教官として処理されず、通常のスタッフとして配置される。そのため教官に固定席・
要サポートを設定していても矛盾ではない。OJT対象者が出勤している日は教官・OJTの処理が
優先されるが、これは「その日は教官として働く」という正常な状態であり、日によって
変わるものとして扱う。

※ **教官 × 新人（rookie.csv）は、B-11として別に中断する**〈ver0.5.7.4で変更〉。
ver0.5.7.3までは「教官 × 固定席と同じ理由で対象外」としてB-4から除外していたが、
**この理由は新人固定席には当てはまらない。**
固定席は「教官として処理されなければ普通に効く」だけだが、新人固定席は
**教官・OJTより先に処理されるため、教官として処理されるべき日に教官の処理そのものを壊す**
（教官が先に着席し、`available` が false になって「担当教官が不在」扱いになる）。
これはB-6（教官に優先フラグ）と同じ機序なので、B-4ではなくB-11として、
B-6と同じ条件・同じ文言の形で中断する。B-4の判定（`held` の組み立て）は変更していない。

※ **固定席の「夜勤GL席」。** 夜勤GL席は**夜勤GL枠**を指名するもので、他の4つは
**座席1〜15**を指名するもの。行き先の枠が違い、夜勤GL枠に入る日はその人は座席1〜15に
並ばない。教官 × 固定席と同じ「日によって変わる正常な状態」として扱う。
なお同じ行に座席番号も書かれていれば（例:「夜勤GL席 3」）、その番号のほうがB-4に
引っかかるため、見逃しにはならない。
実装上は `designatedSeatsMap` に座席番号の指定だけが入るため、そのまま判定に使える。

#### rookie.csv の人が役席・GLで出勤する日との関係

その日 役席・GL として出勤する人が rookie.csv に載っているだけなら**警告のみ**（§5）。
ただし**同じ人に固定席・要サポート・OJTのどれか1つでも付いていればB-4で中断する。**
新人自体が4つのうちの1つなので、他の1つと重なった時点で2つになる。
§5が扱うのは「新人固定席が効かない」という単独の話で、CSV内の矛盾ではないため区別する。

#### 備忘：新人に座席を指定したい場合

優先フラグを代用させない。優先フラグは処理の順番を早めるだけのもので、
座席指定の正式な手段にすると上の一文が崩れる。

**将来、新人の座席を指定したいという要請が出たら、ojt.csv のように対象座席を
書ける形式へ変更して対応する。**
マニュアルには「新人の座席を（ojt.csvのように）指定したい場合は、開発者に
連絡してください」と記載済み。B-4のメッセージにも同じ案内を入れている（§6の文言）。

#### 新人 × OJT について

OJT期間中は rookie.csv に載せない運用のため、両方に載っている場合は ojt.csv の
削除し忘れである。OJTを卒業した人が「新人として1人で着台する」次の段階に進むという
業務の流れと一致する。

**なぜ気づけないか（新人 × OJT の例）：** 新人固定席の処理が先に走り、
その時点で配置済みの人は教官・OJT処理の対象一覧から黙って除外される。
結果は「OJT対象者が本日休み」の場合と見分けがつかず、
教官バッジが消えることも正常動作として説明されている。

### B-5 固定席・要サポートで指定した座席が、同じ人の禁止席にも入っている

論理矛盾。実行はできるが、ver0.5.6 までは
「◯◯さんの配置条件をよく確認してください（指定された座席に配置できません）」
という理由の分からないメッセージしか出なかった。

CSVの時点で「5番が固定席と禁止席の両方に指定されています」と言えれば一発で直せる。

**部分重複でも中断する。** 固定席3席のうち1席だけが禁止席と重なる場合、その人は
残りの席に座れてしまうため座席表を見ても気づけないが、**候補が黙って1つ減っている。**
直し方はどちらか一方を消すことで一意に決まるため止めてよい。

### B-6 教官・OJT対象者に優先フラグが付いている

優先フラグの処理は全ルールの中で最初に走る。そのため教官・OJTの処理に入る時点で
どちらか一方が既に着席済みとなり、**2人を隣り合わせられない。**

- **教官に優先フラグ** … 教官は「本日不在」として扱われ、OJT対象者は別の教官へ
  振り分けられる。**表示されるのは「担当教官が本日不在のため」という事実と異なる
  メッセージ**で、原因にたどり着けない
- **OJT対象者に優先フラグ** … その対象者は先に1人で着席し、教官の担当一覧から
  黙って外れる。教官は担当者がいないものとして通常のスタッフとして配置される。
  **こちらはメッセージが一切出ない**

**中断するのは、教官・OJTの処理が実際に動く日だけ。**
B-1〜B-5・B-7・B-9が出勤者全員を対象にするのに対し、B-6だけは
「その日、座席1〜15に並ぶ人（`seatSideNames`）」で判定する。

- 教官に優先フラグ → 教官本人と担当するOJT対象者の少なくとも1名が、ともに座席側にいる日
- OJT対象者に優先フラグ → 本人が座席側にいて、かつ教官の誰かが座席側にいる日

担当するOJT対象者が全員休みの日は、教官は通常のスタッフとして配置されるため
優先フラグを付けても矛盾にならない（§4の「教官 × 固定席」と同じ扱い）。

なお「OJT対象者に優先フラグだけ（種別なし）」はB-9、
「OJT対象者に優先フラグ＋固定席」はB-4で拾われるため、B-6が単独で効くのは
「優先フラグ＋禁止席／隣接禁止」の場合。

※ **新人に優先フラグは許容する。** 併記できるのは禁止席と隣接禁止の2つ
（固定席・要サポートとの併記はB-4で中断、種別なしはB-9で中断）。
新人固定席は効かなくなるが、それを承知で使う分には止めない。
座席を指定したい場合だけ、B-4で開発者への連絡を案内する。

### B-7 禁止席の指定により、その日その人が座れる席がゼロになる

入力ミス以外にあり得ない。

判定は「禁止席の欄に15個書かれているか」を数えるのではなく、
**禁止席を除いた残りの中に、その人を自動配置できる席が1つでも残っているか**で行う。
理由は2つ。

- **座席9番は通常の自動配置では使われない**（固定席などで名指しされたときだけ使う例外席）。
  そのため禁止席に1〜8番と10〜15番の**14個**を書くと、残るのは9番だけとなり、
  実質ゼロになる。個数を数える方法では見逃す
- 同じ行に同じ番号を重ねて書いた場合（`1 1 2 3 …`）はツール側で1つにまとめるため、
  書かれた個数と実際の禁止席数が一致しない

「残り席がゼロか」で見れば、どちらも自然に拾える。
実装は `algorithm.js` の `remainingSeatKeysAfterForbidden(name, indexes)`。
**固定席・要サポートで座席9番を名指ししている場合は、9番も残り席として数える**
（明示指定は座席9番の除外を上書きするため）。

なお `detectStaticContradictions` のA（禁止席が全15席）とB（指定が全部禁止席と重複）は、
B-7・B-5が読み込んだCSVの内容だけで先に検出するため通常は到達しない。
保存データの読み込み後など、CSVチェックを通らない経路のための保険として残してある。

### B-8 選択日の行が読み飛ばされた（月間シフトCSV）

読み飛ばされた人は座席表から丸ごと消える。**ツールには人を後から追加する機能がないため、
続行しても復旧できない。**

月間シフトCSVはExcelからVBAで出力しているため、1行だけ読み飛ばされるのは
書式の崩れではなく元Excelのデータ不備（氏名の空欄、夜勤の終了時刻を8:00と入力など）に限られ、
発生頻度は低い。修正はExcelを直して再出力するだけで、数分で確実に終わる。

`parseShiftMonthlyRows` が読み飛ばした行を `skipped` として返す。
1件は `{ rowNumber, date, name, reason }`。対象は次の6つ。

| 読み飛ばす理由 | date |
|---|---|
| 日付を読み取れなかった | `null` |
| 氏名が空欄 | 判明している |
| 時刻を読み取れなかった | 判明している |
| 終了時刻が開始時刻以前 | 判明している |
| 役割が役席・GL・OP以外 | 判明している |
| 時間帯の重なる勤務が複数回（`warn` へ変更） | 判明している |

**`date` が `null` の行は、選択日を問わず中断する。** どの日の勤務か判別できないため。
メッセージにも「どの日の勤務か判別できないため、日付を問わず中断しています」と添える。

時間帯の重なる勤務は ver0.5.6 まで `error` で出していたが、赤の意味を
「座席表ができていない」に統一したため `warn` に変更した。選択日の行なら
B-8として中断するため、実害の検出はむしろ強くなっている。

**日付だけが入っていて他が空欄の行も中断する**〈ver0.5.7.1で確認・現状維持〉。
完全な空行の判定は「日付も氏名も空」なので、`2026-06-01,,,,,,` のような行は
「氏名が空欄」として読み飛ばされ、その日は中断になる。空行として黙って読み飛ばす案も
検討したが、**VBAはそうした行を出力しない**ため、出たとすればマクロの不具合か
人がCSVを直しておかしくなった場合に限られる。中断すれば異常に気づけるので中断でよい。

### B-9 優先フラグだけが効いてしまう行

secret.csv で**優先フラグは登録できたのに、その行が座席に関する指定を1つも作れなかった**行。

**ver0.5.6 の挙動：** 優先フラグの登録処理は種別の判定より前に走るため、
**種別が空欄でも優先フラグは実際に効く。** 同時に「種別「」は認識できません」という
警告も出る。つまり黄色の警告は出るが、配置は優先フラグが効いた状態で完成する。

**中断する理由：** 気づけないからではなく（警告は出ている）、
**優先フラグだけが効いて、意図した固定席とは違う座席表が完成してしまうため。**
警告は読み飛ばし系のメッセージに埋もれやすく、座席表を見てもその人が普通に
座っているだけなので、指定したつもりの席を覚えている本人以外は確認できない。

**直し方は2つで、どちらも一意に決まる。**

- 意図した種別と対象座席を正しく書く
- 優先フラグを消す

#### 判定のしかた〈ver0.5.7.1で変更〉

ver0.5.7 では「**種別が空欄・誤字**の行」だけを対象にしていた。しかし種別が正しくても、
その行が読み飛ばされれば残るのは優先フラグだけで、**結果はまったく同じ**になる。

| 入力例 | ver0.5.7 | ver0.5.7.1 |
|---|---|---|
| `,山田 太郎,,,1`（種別が空欄） | 中断 | 中断 |
| `固定,山田 太郎,,3,1`（種別が誤字） | 中断 | 中断 |
| `固定席,山田 太郎,,,1`（対象座席が空欄） | 黄の警告のみ | **中断** |
| `隣接禁止,山田 太郎,,,1`（対象2が空欄） | 黄の警告のみ | **中断** |
| `禁止席,山田 太郎,,7番,1`（座席番号が不正） | 黄の警告のみ | **中断** |
| `禁止席,山田 太郎,,夜勤GL席,1`（種別で使えない値） | 黄の警告のみ | **中断** |
| `固定席,山田 太郎,,3,1`（正常） | 通常 | 通常 |
| `固定席,山田 太郎,,夜勤GL席,1`（夜勤GL枠の指定） | 通常 | 通常 |

そこで判定を「種別が未知かどうか」ではなく、
**「その行が `result` に座席関係の要素を1つでも追加したかどうか」**に変えた。
種別ごとの解析は `parseTypeCell` として独立させ、呼び出し前後の件数を比べている。
この方法なら、今後どんな読み飛ばし条件が増えても自動的に追従する。

`parseSecretRows` が `priorityOnlyRows`（`{ rowNumber, name, typeCell, typeKnown }`）を返す。
`typeCell` / `typeKnown` は、呼び出し側がメッセージの書き出しを入力内容に合わせて
変えるために使う（§6のB-9の文言）。

#### 優先フラグの値が数値でない場合は弾く

`1位` `最優先` `○` などの値は、**その優先フラグ自体を無視**して黄の警告を出す
（従来どおり。「最下位として処理する」案は §7 の不採用案を参照）。
優先フラグが登録されないので、**B-9の対象にもならない。**

これに合わせて、**種別の警告を抑止する条件を変更した**〈ver0.5.7.1〉。
ver0.5.7 は「優先フラグ欄に何か書いてある行」で抑止していたため、
`固定,山田 太郎,,3,○` のように**種別の誤字と優先フラグの誤りが重なると、
種別の誤字が誰にも報告されない**状態になっていた。
抑止の条件を「**優先フラグを実際に登録できた行**」に変えることで、
B-9が出る行では二重に出さず、B-9が出ない行では必ず種別の誤字が伝わる。

※ **対象1（氏名）が空欄で優先フラグだけの行は、すでに無視＋警告になっている。**
優先フラグが登録されないため、B-9の対象外。

---

### B-10 ojt.csv：同じ人が「教官」と「OJT対象者」の両方に書かれている

〈ver0.5.7.4で追加〉

```
教官名,OJT一人目,OJT二人目,対象座席
甲,乙,,
丙,丁,甲,        ← 甲が2行目のOJT二人目にも書かれている
```

甲は**教官としての同席で1席、OJT対象者としての同席でもう1席**を占め、
**同じ方のカードが座席表に2枚できる**（ver0.4.0で廃止したはずの二重配置）。
同じ座席の1人目・2人目に同一人物が入るケースもある。

同じ行の中で `教官名 == OJT対象者` の場合は ver0.5.3 から読み飛ばしていたが、
**行をまたぐ場合は素通りしていた。** B-2（教官の重複）・B-3（対象者の重複）・
B-4（席を指名するルールの重複）のいずれにも当たらず、警告も出ない。

#### なぜ気づけないか

座席表に同じ氏名のカードが2枚並ぶが、**同日2回勤務（応援勤務）の正常な表示と見分けが
つかない。** 2回勤務は氏名が同じで時刻が違うのに対しこちらは時刻まで同じだが、
印刷した紙の上でそこまで見比べる前提にはできない。

#### 判定

| 条件 | 中断するか |
|---|---|
| 兼務している本人が選択日に出勤している | **中断** |
| 本人は休みで、関係者だけ出勤している | 中断しない（二重配置は起きないため） |

実装は `csv.js` の `parseOjtRows` が `mentorTraineeConflicts`
（`{ name, traineeNames, mentorNames }`）を返し、`ui.js` の `collectBlockingProblems` が
出勤者に絞って中断する（B-2・B-3と同じ分担）。照合は `nameKey`（空白を除いた氏名）。

### B-11 ojt.csv の教官が rookie.csv にも登録されている

〈ver0.5.7.4で追加〉

新人固定席（`stepRookies`）は教官・OJT（`stepOjt`）より**先に**処理される。
教官が rookie.csv に載っていると、教官・OJTの処理に入る時点で既に着席済みとなり、
`available = !placedNames.has(mentor.pkey)` が false になる。

結果、**出勤しているのに「担当教官がこの座席表にいない」として扱われ**、
OJT対象者は別の教官へ振り分けられるか、教官なしで配置される。

**B-6（教官に優先フラグ）とまったく同じ機序**のため、中断条件も文言もB-6に揃えてある。

#### 中断するのは、教官・OJTの処理が実際に動く日だけ

- 教官本人が座席側（`seatSideNames`）にいる
- かつ、その教官の担当するOJT対象者の少なくとも1名が座席側にいる

担当するOJT対象者がその日1人も出勤していなければ、教官は通常のスタッフとして
配置されるため中断しない（§4の「教官 × 固定席」と同じ扱い）。

#### 直し方が一つに決まるか

決まる。**rookie.csv からその人の行を削除する。**
§5の「rookie.csv の人が役席・GLで出勤する」ケースとは違い、
教官を務める人が新人として扱われる日があってはならないため、逃げ道は不要。
教官の座席を指定したい場合は ojt.csv の「対象座席」列を使う（B-6と同じ案内）。

---

## 5. 【C】続行してメッセージ欄に表示

| 条件 | 扱い | 状態 |
|---|---|---|
| 選択日**以外**の行のエラー | その日の配置に無関係 | 実装済み |
| 残業種別の表記ゆれ | 「残業なし」に倒すだけ | 実装済み |
| 同日2回勤務 | 応援勤務の可能性があるため注意喚起のみ | 実装済み |
| 座席番号の書式ミス・存在しない座席番号 | その指定1件が効かないだけ | 実装済み |
| 「夜勤GL席」を指定された人がその日日勤 | 日によって変わる正常な状態 | メッセージ不要 |
| 禁止席が多いが、座れる席は残っている | 座れなければあふれ欄に出るので気づける | 実装済み |
| 席そのものが足りずにあふれた（ルールを全部外しても座れない） | **通常のあふれ扱い。**オレンジもダイアログも出さない〈ver0.5.8〉 | ver0.5.8で実装 |
| 隣接禁止の対象1と対象2が同じ氏名 | **読み飛ばし＋警告。** 自分自身との隣接禁止は意味がないため。放置すると解が見つからず、全探索が失敗して不安を煽るダイアログが出る。ojt.csvで教官名とOJT対象者が同じ場合の既存の扱いと揃える | ver0.5.7で実装 |
| rookie.csv の人が、その日 役席・GLとして出勤 | **警告。** 下記の理由により中断しない。**日勤側と夜勤側で文言が違う**〈ver0.5.8〉 | ver0.5.7で警告へ戻した |
| 夜勤の役席・GL2人目を座席10へ入れられなかった | **警告。** 内部ルールのため直し方の指示は出さず、実際の行き先を伝える〈ver0.5.8〉 | ver0.5.8で実装 |
| 教官・OJT対象者どうしに隣接禁止が設定されている | **オレンジ（violation）で警告。** 配置先が構造的に隣接席に固定されているため、探し直しでは解決しない。下記参照 | ver0.5.7.4で実装 |
| 教官とOJT対象者が日勤側・夜勤側に分かれて出勤 | **警告。** 座席表が別物のため同席できず、反対側の別の教官へ振り分けられる。ojt.csv の更新忘れの可能性。下記参照 | ver0.5.7.4で実装 |

### rookie.csv の人が役席・GLで出勤するケースを中断しない理由

ver0.5.6 で「設定ミスとみなして配置を実行しない」を実装したが、撤回した。

- 同じ人が**OP勤務の日は新人ルールを効かせたい**。ところが役席・GL勤務の日だけ中断する
- 中断を解除する唯一の方法は rookie.csv の行を消すことで、そうするとOP勤務の日の
  新人ルールまで失われる
- つまり**直し方が一つに決まらない。**「逃げ道が必要なものは止めない」という
  §1の原則に反する

警告では、新人固定席が効かず早番・遅番エリア（または夜勤GL枠）に入ることを理由つきで伝え、
**「対処は不要です。同じ人がOP勤務の日は新人固定席が効くため rookie.csv はそのままに」**
と明示する。マニュアル12章・7章の新人カードも同じ内容に直した。

#### 夜勤側は文言を分ける〈ver0.5.8で修正〉

**夜勤の役席・GLは行き先が2つに分かれる。**`assignNightLeaders` が1名だけを夜勤GL枠へ入れ、
**2人目以降は座席1〜15に並ぶ**（座席側へ回る）。座席側へ回った人には**新人固定席がそのまま効き**、
実際に座席5へ着席して「新人1」バッジまで付く。

ver0.5.7.5までは日勤・夜勤をまとめて「新人固定席は効きません／早番・遅番エリアまたは
夜勤GL枠に配置します」と出していたため、**座席側へ回った人については文言が事実と逆**だった。
現在は3通りに分ける。

| 対象 | 文言 |
|---|---|
| 日勤の役席・GL（`leaderRows`） | 早番・遅番エリアに入るため効かない。対処不要 |
| 夜勤GL枠に入った1名 | 夜勤GL枠に入るため効かない。対処不要（日勤と同じ文） |
| 夜勤で座席側へ回った2人目以降 | **座席1〜15に並ぶため新人固定席がそのまま効く。**「新人」バッジも付く。役席・GLの日は新人扱いにしたくない場合は作成者へ連絡 |

行き先は `assignNightLeaders` を呼ぶまで決まらないため、**夜勤側の警告はその戻り値
（`glState['2-1']` と `seatLeaders`）が出てから出す。** 日勤側は従来どおり `splitDayRows` の
直後に出す。

なお同じ人に固定席・要サポート・OJTのどれかが付いていれば、それはCSV内の矛盾なので
B-4で中断する（§4）。

### 教官・OJT対象者どうしの隣接禁止を中断にしない理由

〈ver0.5.7.4で追加〉

教官＋OJT一人目は「同じ座席」、OJT二人目は「その隣の座席」と、**配置先が構造的に
決まっている**（ペア候補 `OJT_PAIR_*` はすべて隣接席）。したがって、この3人の間の
隣接禁止は「候補を選び直せば避けられる」性質のものではなく、
**同席をやめない限り必ず破られる。**

組み合わせによって扱いを分けた。

| 隣接禁止の組み合わせ | 挙動 | メッセージ |
|---|---|---|
| 教官 × OJT一人目 | 同じ座席に座らせることになるので**同席をやめて個別配置** | オレンジ |
| OJT一人目 × OJT二人目 | 同席をやめても隣接は避けられないため**同席を優先してそのまま配置** | オレンジ |
| 教官 × OJT二人目 | 同上 | オレンジ |

**中断しない理由は、直し方が一つに決まらないから**（§1の原則）。
ojt.csv の組み合わせを変えるのか、secret.csv の隣接禁止を消すのかは業務判断であり、
どちらが正しいかをツールが決められない。

**3人とも個別配置にして隣接禁止を必ず守る案は採らなかった。**
隣接禁止は守れるが、OJTの同席という運用上の目的が黙って失われる。
「座席表はできたが、ルール違反がある」＝オレンジの定義そのものなので、
警告して人の判断に委ねる形にした。

### 教官とOJT対象者が日勤側・夜勤側に分かれる日

〈ver0.5.7.4で追加〉

日勤と夜勤は座席表が完全に別物のため、この2人は同席できない。
夜勤側の計算では日勤の教官が `byName` に存在せず「不在」と見えるため、
**無関係な夜勤の教官へ自動的に振り分けられ**、勤務時間の重なる2人が1席に同居する。

#### 運用上の位置づけ

- **夜勤勤務予定の方が、OJTだけ日中に行うことがよくある。**
  その日は対象者も日勤側に来るため、この分岐には入らない（正常な運用）
- 逆に対象者が**夜勤側に来ている**＝勤務時間そのものが夜勤へ移っているということ。
  本来はこの段階で、次のいずれかへ切り替えているはず
  - 夜勤の教官とペアを組み直す
  - rookie.csv に移す（夜勤特有の運用であれば secret.csv の固定席）
- つまり **ojt.csv の更新忘れの可能性が高い**

#### 中断しない理由

「その日は振り分け先の教官で回す」という判断もあり得るため。
配置の挙動自体も変更していない（従来どおり振り分ける）。

メッセージは勤務時間が重なるかどうかで書き分ける。

| 状況 | 文言の要点 |
|---|---|
| 重ならない（通常こちら） | この組み合わせでのOJTは成立しない。ojt.csv の更新忘れの可能性。夜勤の教官とペアを組み直すか、rookie.csv（または secret.csv の固定席）へ切り替える |
| 重なる（教官の遅番が対象者の夜勤開始に食い込む等） | 日勤側と夜勤側で座席表が分かれるため同席はできない。意図した配置か確認 |

#### あわせて「本日不在」という表現をやめた

`algorithm.js` のメッセージを「**この座席表にいない**」へ変更した。
反対側に出勤している場合もこのメッセージが出るため、「不在」は事実と違う。
ui.js 側で【日勤】【夜勤】の接頭辞が付くので、どちらの表の話かは伝わる。

### 将来の課題：役席・GL採用の新人がOJT研修を受ける場合

現状のツールでは、**役席・GLとして出勤する日は rookie.csv も ojt.csv も効かない。**

- 新人固定席は座席1〜15のルールなので、早番・遅番エリアには効かない
- OJT対象者が役席・GLの日は、教官も座席側へ移らず早番・遅番エリアに残る
  （ver0.5.3で意図的にそうした。OJT対象者が座席1〜15に来ないため同席が成立しないから）

実際にそうした研修が始まる場合は、別途の対応が必要になる。**ver0.5.7では未対応。**

---

## 6. メッセージ文言の方針

### 月間シフトCSVは「Excelを直す」と書く

VBA出力のため、**CSVを直接修正しても次回の出力で消える。**
ver0.5.6 までのメッセージとマニュアルは利用者がCSVを直す前提の文言だったため、
全面的に見直した。

> 月間シフトCSV 15行目：開始時刻が終了時刻以降になっているため読み飛ばしました（田中 花子）。
> 夜勤は終了時刻を24時以降の表記（例: 翌8:00は32:00）で入力してください。
> **元のExcelを修正し、CSVを出力し直してください。**

マニュアル12章では、日付・時刻・役割の3行を1つの対処セルにまとめた
（原因は違っても対処は「Excelを直して出力し直す」の1つだけのため）。

### 同じエラーが毎月出るならVBA側を直す

夜勤の終了時刻が毎回 `8:00` で出るなら、それはデータ不備ではなく出力ロジックの問題で、
VBA側で `32:00` と書くようにすれば恒久的に消える。役割名の表記も同じ。

エラーが出たら、**単発のデータ不備か、毎月出る出力仕様の問題か**を切り分ける。
後者はVBAの修正依頼が正解。マニュアル12章にも1行入れた。

### B-4 の文言

種別名を先に置く。現場の方が最初に見るのは自分が書いた種別名のため。
先頭のファイル名は、当たっているルールの出どころから組み立てる。

> rookie.csv / secret.csv：「山田太郎」さんは、新人と固定席に登録されています。
> 固定席・要サポート・新人・OJTは、1人にどれか1つだけです。この4つはどれも
> 「座る席を指名する」ものなので、2つ以上あると後のほう（固定席）が無視されます。

**「の両方に」は付けない**〈ver0.5.7.1で修正〉。ルールは4つあるので3つ以上当たることが
あり、「新人とOJT対象者と固定席の**両方**に登録されています」と日本語が崩れる。
「〜に登録されています」なら2つでも3つでも成立するため、件数による出し分けは不要。
マニュアル12章の中断表も同じ文言に揃えた。

新人 × 固定席／要サポートの場合だけ、次の1行を足す。
正しい行き先を示さないと、優先フラグを付けて回避しようとされる。

> **新人の座席を指定したい場合は、優先フラグではなく作成者にご連絡ください**
> （対応方法をご相談します）。

※ 禁止席・隣接禁止が併記できることは**メッセージには書かない。** 4つの種別を
並べてあるので、そこに禁止席が無いこと自体が答えになる。エラー文で無関係な種別に
触れると「禁止席も直すのか」と読ませてしまう。マニュアル側（10章・12章）にだけ書く。

### B-6（教官・OJT対象者の優先フラグ）の文言

理解されにくい内容のため、丁寧に説明する。

**教官に付いている場合：**

> secret.csv：「山田 太郎」さんは ojt.csv で教官として登録されていますが、
> 優先フラグも設定されています。
>
> 優先フラグを付けた人は、**他のどのルールよりも先に、1人だけで席が決まります。**
> そのため教官として処理される時点では既に着席済みとなり、
> OJT対象者と隣り合わせることができません。
> この状態では「担当教官が本日不在」として扱われ、OJT対象者は別の教官に振り分けられます。
>
> 教官として使う場合は、優先フラグを削除してください。
> **教官の座席を指定したい場合は、ojt.csv の「対象座席」列をお使いください。**

最後の1行が要点。優先フラグを使いたくなる動機は「この教官をこの席に」だと考えられるため、
正しい方法を示さないと削除してもらえない。

**OJT対象者に付いている場合：**

> secret.csv：「佐藤 次郎」さんは ojt.csv でOJT対象者として登録されていますが、
> 優先フラグも設定されています。
>
> 優先フラグを付けた人は、**他のどのルールよりも先に、1人だけで席が決まります。**
> そのため教官（山田 太郎さん）と隣り合わせることができず、
> 教官は担当者がいないものとして配置されます。
>
> 優先フラグを削除してください。
> **同席する座席を指定したい場合は、ojt.csv の「対象座席」列をお使いください。**

### B-9 の文言

**書き出しだけ、入力内容に合わせて3通りに出し分ける**〈ver0.5.7.1で変更〉。
一律に「種別が書かれていません」と出すと、実際には「固定」などと**書いている**人には
内容が食い違って見え、どこを直せばよいか分からない。

| 入力の状態 | 書き出し |
|---|---|
| 種別欄が空 | 「◯◯」さんの行は優先フラグだけが設定されていて、種別（固定席・禁止席・要サポート）が書かれていません。 |
| 種別欄が誤字 | 「◯◯」さんの行は種別「固定」を認識できないため、優先フラグだけが設定された状態になっています。 |
| 種別は正しいが行が読み飛ばされた | 「◯◯」さんの行は「固定席」の指定を読み取れなかったため（同じ行について黄色のメッセージが出ています）、優先フラグだけが設定された状態になっています。 |

3つ目は、何が足りないかを黄色のメッセージ側が既に具体的に言っている
（「固定席の情報が不足しています」「「7番」は座席番号として認識できません」など）。
赤の側で繰り返さず、同じ行の黄色を見るよう促すだけにする。

以降の本文は共通。

> 優先フラグは座席を決めるものではなく、**処理の順番を早めるだけ**のものです。
> このままでは、この人が先にどこか空いている席に座るだけになります。
>
> 種別と対象座席を正しく書くか、優先フラグを消してください。

### B-10（教官とOJT対象者の兼務）の文言

> ojt.csv：「甲」さんが、教官（担当するOJT対象者: 乙）としても、
> OJT対象者（担当教官: 丙）としても書かれています。
>
> 同じ方が両方に書かれていると、教官としての同席で1席、OJT対象者としての同席で
> もう1席を占めるため、**同じ方のカードが座席表に2枚できてしまいます。**
>
> どちらか一方の行を修正してください
> （**OJTを卒業して教官になった場合は、対象者として書かれている行を消してください**）。

最後の括弧が要点。この状態になる原因はほぼ「OJTを卒業した人を教官として追記したが、
対象者として書かれていた古い行を消し忘れた」であり、どちらを消すかを迷わせない。

### B-11（教官が rookie.csv にも登録）の文言

B-6（教官に優先フラグ）と同じ構成にする。**最後の1行が要点**である点も同じ。

> rookie.csv / ojt.csv：「山田 太郎」さんは ojt.csv で教官として登録されていますが、
> rookie.csv にも登録されています。
>
> 新人固定席は教官・OJTより先に処理されるため、教官として処理される時点では
> 既に着席済みとなり、OJT対象者と隣り合わせることができません。
> この状態では「担当教官がこの座席表にいない」として扱われ、
> OJT対象者は別の教官に振り分けられます（**実際には出勤しているため、表示とも食い違います**）。
>
> 教官として使う場合は、rookie.csv からこの方の行を削除してください。
> **教官の座席を指定したい場合は、ojt.csv の「対象座席」列をお使いください。**

### 中断ダイアログの文言

詳細はメッセージ欄で読ませるため、件数と行き先だけを伝える。
**読み込み時（A）のダイアログも ver0.5.7.1 でこの形に揃えた**（§3を参照）。

> 入力内容に問題があるため、自動配置を実行できませんでした（3件）。
>
> 内容は画面の「2. メッセージ」欄（赤色）に表示しています。修正してから、
> もう一度実行してください。

**件数は「メッセージ欄に出る赤いブロックの数」**であり、問題そのものの件数ではない
〈ver0.5.7.1で確認・現状維持〉。B-8だけは何行読み飛ばしても1ブロックにまとめるため、
5行読み飛ばしても「1件」と出る。数字が指しているのは「読むべき塊の数」で、実際に
その数だけ表示されるため、これで筋が通っている。

---

## 7. 検討したが不採用にした案

同じ議論を繰り返さないための記録。

| 案 | 不採用の理由 |
|---|---|
| **B-4〜B-7・B-9の対象を「その日、座席1〜15に並ぶ人」に絞る**（2026-07-30に一度採用し撤回） | バッジはCSVに書かれているかどうかだけで決まり（`deriveBadgeFields`）、早番・遅番エリアへ回った人にも付けているため（`reapplyLeaderBadges`）、見逃した矛盾が**手動で座席へ動かした時点で説明なしのバッジとして現れる。** 例えば種別なしの優先フラグを見逃すと「優先」バッジだけが単独で付いたカードができ、しかもその表示は事実と違う（手で動かした人であって、最優先で席が決まった人ではない）。B-4なら「新人1」と「固定席」、B-5なら「固定席 3番」と「禁止席 3番」が同時に付く。「バッジは配置状態ではなくCSVの記載で決まる」という既存の原則と衝突するため撤回し、**出勤者全員**に戻した |
| **CSV解析を引用符（RFC 4180）対応に作り直す** | すでに対応済みだった。`csv.js` のパーサーは1文字ずつ走査し、引用符・`""`・引用符内の改行を扱える。作り直すと、空行の判定範囲が変わって現在出ている警告が消えるなど、直す必要のない箇所が変わる |
| **rookie.csv の人が役席・GLで出勤したら中断**（ver0.5.6で実装、2026-07-30に撤回） | OP勤務の日は新人ルールを効かせたいのに、役席・GL勤務の日だけ止まる。解除するには行を消すしかなく、直し方が一つに決まらない。警告に戻した |
| **secret / ojt / rookie の氏名が月間シフトCSVに一度も出てこない場合に中断** | 数か月休職中の人は月間シフトCSVに載らないため、正しく書けている氏名と打ち間違いを区別できない。判定できないものを中断条件にすると、逃げ道の設計が延々と必要になる |
| **「一時的に除外」列の追加** | 上記の中断条件の逃げ道として設計したもの。中断しないと決めた時点で、休職者の行は書いてあっても静かに効かないだけになり、実害がないため不要 |
| **除外列の怪しい値（「なし」「－」等）の警告／読み込み件数の表示** | 上記に付随して不要 |
| **氏名の表記ゆれ警告** | 姓名間のスペースを正規化して吸収する方針に変更したため、警告を出す前に一致する |
| **氏名照合を `NameMap` / `NameSet` でラップする** | 照合箇所が約60か所あり置き換え漏れのリスクが大きい。CSVを読む入口で氏名を書き換える方式に変更（ver0.5.6で実装済み） |
| **同じ座席番号を2人以上が固定席指定** | 手動で調整すればよい |
| **隣接禁止のペアが両方とも隣同士の席に固定席指定されている** | 検出は可能だが、座席の隣接関係を総当たりする必要があり、実装量の割に発生頻度が低い |
| **`#` などのマークで行を無効化する** | rookie.csv・ojt.csv では氏名そのものを書き換えることになり、氏名不一致という最多のミスを自分から作りにいくことになる（専用列案を経て、最終的に列ごと不採用） |
| **新人×優先フラグ付きの固定席／要サポートを中断から除外する案**（2026-07-30に一度採用し撤回） | 優先フラグは処理の順番を早めるだけのもので、座席指定の正式な手段にすると「座る席を指名するルールは1人に1つだけ」という説明が崩れる。新人の座席指定は開発者への連絡に一本化し、要請が出た時点でrookie.csvを座席指定できる形式に変更する |
| **B-5を「指定席の全部が禁止席と重複したときだけ中断」にする** | 部分重複でも候補が黙って1つ減り、座席表を見て気づけない。直し方はどちらか一方を消すことで一意に決まるため、部分重複でも止める |
| **優先フラグの値が数値でないとき、最下位として処理する**（ver0.5.7.1で検討） | 書いた本人は効いているつもりなのに、実際の順番は他の指定に埋もれる。座席表を見ても気づけない。弾けばその人はどのルールも効かない普通の配置になり、**警告の内容と結果が一致する** |
| **中断ダイアログを1件のときだけ全文表示する**（ver0.5.7.1で検討） | 件数で挙動が変わると「なぜ今回は中身が出ないのか」を説明することになる。A-1の本文は列の比較を含むため1ファイルでもダイアログ向きではなく、メッセージ欄のほうが読みやすい（改行がそのまま出る・コピーできる・消えない）。**常に短くし、ボタンでの読み込みも揃える** |
| **読み込み時の不合格でも画面の座席表を消す**（ver0.5.7.1で検討） | 実行時の中断で消すのは「作り直す」と言われたため。読み込み時はまだ頼まれていない。表示中の座席表は正しく日付ラベルも一致しており、消すと翌月分のCSVを読み間違えただけでその日の成果が失われる |
| **B-8の中断件数を読み飛ばした行数で数える**（ver0.5.7.1で検討） | 件数が指しているのは「メッセージ欄に出る赤いブロックの数」で、実際にその数だけ表示される。数え方を変えると、かえって表示との対応が崩れる |
| **日付だけが入った行を空行として黙って読み飛ばす**（ver0.5.7.1で検討） | VBAはそうした行を出力しない。出たとすればマクロの不具合か人がCSVを直しておかしくした場合で、中断すれば異常に気づける |


---

## 8. その他の仕様

### バッジは secret.csv の記載だけで決まる（silent 行）

夜勤の役席・GLが2名以上の日、座席側へ回る先頭の1名は**座席10へ強制配置**する。
実装は `ui.js` が secret.csv の行に `{ type:'seat_designated', silent:true }` を
**その配置限定で1行足す**方式で、secret.csv 自体は変更しない。

`silent:true` の行は「利用者が書いた指定ではない」ため、**バッジには一切現れない。**
`buildSecretIndexes` は2つのマップを返し、用途で使い分ける〈ver0.5.7.5で分離〉。

| 名前 | silent行 | 用途 |
|---|---|---|
| `designatedSeatsMap` | **含む** | 配置本体・矛盾検知（`detectStaticContradictions`） |
| `designatedSeatsMapForBadge` | 含まない | 「固定席」バッジの座席番号（`designatedSeatNumbers`） |

`designatedNames`（バッジを出すかどうか）は以前から silent を除いていたが、
座席番号のほうが `designatedSeatsMap` を見ていたため、**secret.csv で座席10を
固定席に指定した人がそのまま座席10へ回ると「固定席 10・10」と重複表示**されていた。

これは §7 の「バッジは配置状態ではなく **CSVの記載** で決まる」という原則の一件で、
バッジ用の値は必ず `〜ForBadge` 側から作る。`ui.js` の `deriveBadgeFields`
（手入力での氏名変更・枠から座席への移動）も同じマップを参照する。

#### silent 行は**メッセージにも現れない**〈ver0.5.8で追加〉

バッジと同じ理由で、**利用者向けのメッセージでも silent 行に触れない。**
secret.csv に書いていない指定を名指しされても、どこを直せばよいのか分からないため。

| 場所 | ver0.5.7.5までの挙動 | 現在 |
|---|---|---|
| `stepDesignated` の「指定された座席に配置できません」 | silent行しか持たない人にも出ていた | `designatedSeatsMapForBadge` に載っている人（＝利用者が書いた固定席がある人）にだけ出す |
| `detectStaticContradictions` のB（指定が全部禁止席と重複）・C（同一座席の単独候補が3名以上） | silent行も対象で、「固定席指定（2-3）は…」と内部キーまで出ていた | silent行だけの人は対象外。座席の表記も**座席番号**（`numberOfKey`）に統一 |

#### 代わりに、座席10へ入れられたかどうかを1件だけ知らせる〈ver0.5.8で追加〉

silent 行を黙らせただけだと、**「2人目は座席10」が通らなかったことに誰も気づけない。**
そこで `ui.js` が夜勤の配置後に結果を確かめ、座席10に入っていなければ実際の行き先を添えて
`warn`（黄）で1件出す。

> 【夜勤】役席・GLが2名以上のため「◯◯」さんを座席10へ入れようとしましたが、
> 座席10には入れられず、座席12番に配置しました。先に処理されるルール（新人固定席・
> 優先フラグ・本人の固定席／禁止席・他の方の固定席10など）が優先されたためです。
> 座席10に入れたい場合は、座席表で手動で入れ替えてください。

**中断はしない。** 本人の固定席が優先されただけ、というCSVの矛盾ではないケースを含み、
直し方も一つに決まらない（§1）。通らなくなる原因は次のとおり。

| 原因 | ver0.5.7.5までの見え方 |
|---|---|
| 本人が rookie.csv に載っている（座席5へ） | 何も出ない＋新人の警告が事実と逆 |
| 本人が別の固定席を持っている（座席12へ） | **何も出ない** |
| 本人が座席10を禁止席にしている | 「固定席指定（2-3）は…」＋「指定された座席に配置できません」 |
| 他の方が固定席10を持っている | shift.csvの行順で勝敗が変わり、負けた側にだけメッセージ／または何も出ない |

### 時刻の範囲〈ver0.5.8で追加〉

| 欄 | 認める範囲 |
|---|---|
| 開始時刻 | **0:00〜23:59。** その行の日付当日のため、24時以降の延長表記は使えない |
| 終了時刻 | **0:00〜47:59。** 日をまたぐ夜勤のため24時以降の延長表記を認めるが、翌日中まで |

範囲外は読み飛ばして理由を伝える（＝B-8で中断する）。`30:00` `48:00` `99:00` のような
入力ミスをそのまま通すと、**勤務時間の重なり判定・夜勤判定（開始が22:00より遅い、
または終了が26:00より遅い）が丸ごと狂う**が、画面上は普通に見えてしまうため。

### 数値を書く欄の書式〈ver0.5.8で統一〉

数値を書く欄は**すべて「0以上の整数」**とし、**半角・全角どちらの数字でもよい**
（`normalizeDigits` で半角に直してから `/^\d+$/` で判定する）。

| 欄 | ver0.5.7.5まで | 現在 |
|---|---|---|
| secret.csv 優先フラグ | 整数のみ（従来どおり） | 整数のみ |
| rookie.csv 新人度合い | `parseFloat` のため**小数も通り**、`3級` も 3 として通っていた | **整数のみ。**小数・数字混じりの文字列は読み飛ばし＋警告 |

新人度合いは順位付けにしか使わないため、小数を認める理由がない。
書式が2種類あると「数値」の説明がマニュアル上でも分かれてしまうため、優先フラグに揃えた。

### 「置けない人」をオレンジにするかどうか〈ver0.5.8〉

`placeOrOverflow` は、座れる席が見つからなかった人を「あふれ」に入れる。このとき
**オレンジ（violation）＋ダイアログを出すのは、secret.csv のルールが原因のときだけ。**

判定は **「secret.csv のルールを全部外しても、その人が座れる席が残っているか」**
（`seatsAvailableIgnoringRules`＝空の集合で `countValidSeats` を数え直す）。

| 状況 | 扱い |
|---|---|
| ルールを外せば座れる（＝禁止席・隣接禁止が原因） | オレンジ＋ダイアログ。「secret.csvの条件を確認してください」 |
| ルールを外しても座れない（＝席・枠が足りない、勤務時間が重なっている） | **通常のあふれ。**メッセージなし |

ver0.5.7.5までは後者にも「配置ルールに矛盾がある可能性があります。secret.csvの条件を
確認してください。」を出しており、**直しようのない指摘が人数分のダイアログとして続けて
出ていた**（実測で5連続）。席が足りないことはあふれ欄を見れば分かる（§5）。
同じ判定を、配置前の「この時点で座れる座席がありません」の事前チェック2か所にも入れてある。

### 「計算中…」の表示と、探索の分割〈ver0.5.8で追加〉

隣接禁止の解が無い日は、全探索が **1段階5秒×3段階＝最大15秒／片側**（日勤・夜勤の
両方で起きると最大30秒）かかる。探索は同期処理のため、その間ブラウザは操作を受け付けず
進捗も出ず、利用者からは「固まった／壊れた」ようにしか見えなかった。

`assignSeatsWithEscalationAsync`（`assignSeatsWithEscalation` の非同期版）が
**1段階ごとに `onLevel` を await** し、`ui.js` がそこで1回描画を挟む。

| 表示 | タイミング |
|---|---|
| `計算中…` | 重い処理に入る直前 |
| `計算中…（日勤の座席）` | 段階0（通常の優先順位） |
| `計算中…（日勤の座席：隣接禁止の再探索 2/3）` | 段階1以降 |
| `計算中…（夜勤の座席…）` | 夜勤側 |
| `計算時間：◯ms` | 完了時 |

- 探索そのものは同期のまま。**固まる時間は最大でも1段階ぶん（5秒）**になる。
- 探索の順序・内容は同期版とまったく同じで、**配置結果は変わらない**。
- 計算中は「自動配置を実行」「配置違反チェック」「印刷」「保存」を無効化し、
  ハンドラ先頭でも `calcBusy` を見て二重起動を防ぐ。
- 制限時間そのもの（`EXHAUSTIVE_TIME_BUDGET_MS = 5000`）は変えていない。
  解がある日は0.5秒以内に終わるため、通常運用ではこの表示はほぼ見えない。

### 候補の切り替えは「探索時の盤面」をそのまま出す〈ver0.5.8で変更〉

「次案を表示」「候補1に戻す」は、**探索時に組み立てた候補の盤面をそのまま表示する**。

ver0.5.7.5までは候補を切り替えるたびに `reshuffleForbiddenAndOthers` を通しており、
禁止席だけの対象者・その他スタッフが毎回ランダムに並び替わっていた。そのため、

- 「候補1に戻す」を押しても押すたび違う盤面になり、**元の案に戻れない**
  （画面の説明は「候補1へ一発で戻れます」、ボタンの説明は「候補1（最良と判定された案）に戻します」）
- 候補を行き来しても隣接禁止以外の人まで動くので、**案どうしを見比べられない**
- 「一部シャッフル」（禁止席対象者・その他だけを振り直す）と役割が重複する

という状態だった。3つのボタンの役割は次のとおり。

| ボタン | 変わるもの |
|---|---|
| 次案を表示 / 候補1に戻す | **隣接禁止対象者の配置（＝候補そのもの）。**同じ候補なら常に同じ盤面 |
| 一部シャッフル | 禁止席だけの対象者・その他スタッフだけ。候補（`ex.index`）は変えない |

「一部シャッフル」の後に「候補1に戻す」を押すと、候補1の元の盤面に戻る。

### 保存ファイルに入れるもの・入れないもの

`exportPerson` のホワイトリスト方式で、secret.csv から推測できる情報
（固定席・禁止席・隣接禁止・要サポート・夜勤GL席の対象かどうか、その座席番号、
グループ記号）は**保存しない**。読み込み時に、その時点の secret.csv から作り直す。
新人（rookie.csv）と教官・OJT（ojt.csv）のバッジは、対応するCSVが無い環境でも
表示が失われないよう例外として保存する。

**その日の出勤者の氏名（`dayRoster` / `nightRoster`）も保存する**〈ver0.5.8で追加〉。
隣接禁止バッジ・教官バッジの出し分けは「**その日出勤しているか**」で決まり、
「いま盤面のどこにいるか」は見ない（ver0.5.4の方針）。保存ファイルにこの一覧が
無かったため、読み込み後は盤面から出勤者を推定するしかなく、**✎で削除した人・
反対側へ動かした人がいると、相手のバッジが保存前と食い違っていた**
（保存する前の画面には出ていた「隣禁止」バッジが、読み込むと消える）。
月間シフトCSV相当の氏名であり secret.csv 由来ではないため、保存してよい。

`formatVersion` は **1のまま**据え置く。この2項目は「無ければ従来どおり盤面から推定」に
落ちる追加項目で、古いツールで開いても壊れないため、`formatVersion` を上げると
かえって古いツールで開けなくなる（読み込み側は `formatVersion > 1` をエラーにしている）。

### 新人の順位（新人1〜7）の付け直し

順位は「その側に並んでいる新人の中での順」で、`reapplyRookieRanks` が付け直す。
並べ替えは **①新人度合い → ②いま付いている順位 → ③盤面の並び** の順で比較する
〈②はver0.5.8で追加〉。

自動配置での同数値のタイブレークは「月間シフトCSVで後ろの行」だが、**その行順は
保存ファイルに残らない。** ②が無かったため、新人度合いが同じ2人がいると
保存→読み込みで「新人1」と「新人2」が入れ替わり、印刷済みの紙と食い違っていた。
順位は保存データにも ✎編集前のカードにも入っているので、それを引き継ぐ。

**早番・遅番エリア／夜勤GL枠・予備枠にいる人は対象に含めない。** 自動配置側も
座席1〜15に並ぶ人だけで順位を決めており、役席・GLとして枠に入る人は新人固定席の
対象外のため（§5）。

### 手動でカードを動かしたときのバッジ

バッジは原則としてCSVの記載だけで決まるため、**カードを動かしても変わらない**
（ver0.5.4の方針。動かすたびに増減すると現場が混乱するため）。例外は次の2つで、
`handleDrop` が動かした直後に付け直す〈ver0.5.8で追加。それ以前は付け直しておらず、
保存して読み込み直した時点で初めて表示が変わっていた〉。

| 項目 | 理由 |
|---|---|
| **「夜勤GL席」バッジ** | 「secret.csvに指定があり、**かつ夜勤側にいる**人」に付ける決まり（ver0.5.6）。側をまたいで動かすと付け外しが要る |
| **新人の順位（新人1〜7）** | 「その側に並んでいる新人の中での順」で決まる。✎編集の保存時は ver0.5.3 から付け直していた |

### 氏名照合のスペース吸収

`csv.js` に実装されている。

- 照合（`nameKey`）：半角・全角・有無をすべて同一とみなす（`\s+` を除去）
- 表示（`displayName`）：連続する空白（改行・タブ・全角スペースを含む）を半角スペース1つに詰める
- 設定ファイル側の氏名は、月間シフトCSVの表記に寄せる（`registerName` / `resolveName`）

全角英数字と半角英数字の同一視、旧字体・異体字の同一視（髙／高など）は対象外。


### 月をまたぐ月間シフトCSVの表示

**年末年始は12月に加えて翌1月上旬までが1つのCSVに入る。** 表示は次のようにする
（`csv.js` の `yearMonthLabelFromDates`、`ui.js` の `populateDateSelect`）。

- 年月表示：複数の年月にまたがる場合は `2025年12月〜2026年1月` と範囲で示す
- 日付のプルダウン：先頭の年月と違う日付には `1月1日（木）` と月を付ける。
  **2つ目以降の月は全件に付ける**（境目の1件だけに付けると、下へスクロールした人には
  それが何月なのか分からず、直したい間違いがそのまま残るため）
- 単月のCSVでは従来どおり `2025年12月` ＋ `1日（月）` の見た目になる

日付そのものの扱い（`rowsForDate` 等）は元から `YYYY-MM-DD` で持っており、
配置・印刷・保存の各機能は月をまたいでも正しく動作していた。表示だけの問題。

---

## 9. 座席カードの寸法（ver0.5.8時点）

### 現在の値

`style.css` の実際の値。**§9の数値は style.css を正とする**〈ver0.5.8で実物に合わせて書き直した。
ver0.5.7.4〜0.5.7.5の記述（1マス224px・`.info` 124px 等）は実装と食い違っていた〉。

| 項目 | 値 |
|---|---|
| `.seat-grid` の1マス | **202px**（`grid-template-columns:repeat(4, 202px)`） |
| `.seat-grid` の `gap` | 14px |
| `.person-card .info` | **105px** |
| `.person-card` の `gap` | **10px** |
| `.ot-time` の `padding` | **0 3px** |
| `.leader-area` | **418px** |

座席1マス202pxの内訳:

```
202 =  18 (.seat の左右padding 9px×2)
     +  2 (.seat の border)
     +  3 (.slot の border 1.5px×2)
     + 16 (.person-card の左右padding 8px×2)
     +  3 (.person-card の border 1.5px×2)
     + 105 (.info)
     +  10 (gap)
     ＝ 157 → バッジ列に残るのは 45px
```

- 座席グリッドの幅は `202×4 + 14×3 = 850px`。早番・遅番エリアも `418×2 + 14 = 850px` で
  横幅がそろっている。
- 3文字バッジ（「固定席」「禁止席」「隣禁止」「要サポ」）は約49pxで、割り当ての45pxを
  4px超える。ただし `.person-card` の右padding 8px の内側に収まるため、**カードの枠外へは
  はみ出さない。**

### なぜ幅が問題になるか

`.info` は**編集ボタン（✎）の位置をカード間でそろえるための固定幅**で、時刻行は
等幅フォント16px・`white-space:nowrap`・**`overflow` の指定なし**。そのため `.info` の幅を
超えた分は、右隣のバッジ列の上に描画される。

Chromiumでの実測（時刻テキストの実インク幅。`.ot-time` の padding `0 3px` 込み）:

| 表示例 | Consolas（Windows既定） | Courier New（Consolasが無い環境） |
|---|---|---|
| `8:00-17:00` | 87px | 96px |
| `10:00-19:00` / `22:30-32:00` | 96px | 105px |
| `9:00※-18:00`（残業1つ） | 104px | 113px |
| **`22:30※-32:00`（最長）** | **113px** | **122px** |

**残業は前・後のどちらか一方しか付かない**（実運用）。したがって最長は
「時・分とも2桁 ＋ 残業マーカー1つ」の `22:30※-32:00`。

### 現在の値での見込み

バッジ列の左端は `.info` の左端から **105 + 10 = 115px** の位置にある。

| フォント | 最長の時刻 | 判定 |
|---|---|---|
| **Consolas（Windows既定。実際の利用環境）** | 113px | **収まる**（余裕 約2px） |
| Courier New | 122px | 約7px 重なる計算になるが、**利用環境の対象外** |

**利用環境はWindowsのみ**（Consolasが必ず入っている）。Courier New へフォールバックする
環境で開く予定はないため、上表の2行目は対応しない。`--font-mono` の先頭が `Consolas` で
ある限りこの前提は変わらない。

### 副作用

- 画面の必要幅は約1110px。1280px以上のブラウザ幅なら横スクロールは出ない
  （1366×768のノートPCでも問題なし）。1024px幅では横スクロールが出る（`課題.md` C）。
- **印刷（`buildPrintHtml`）はmm指定の独立したCSSのため影響を受けない。**

### 幅を変えずに重なりを減らす手

`.ot-time` の `padding` を `0 3px` → `0 2px` にすると、時刻の幅が2px縮む
（最長で Consolas 111px / Courier New 120px 相当）。1行の変更で済む緩和策。
