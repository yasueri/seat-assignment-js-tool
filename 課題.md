# 課題 — 未対応・判断待ち

座席配置ツール **ver0.5.8** 時点。**まだ終わっていないものだけ**を書く。
終わったらこのファイルから消し、`履歴.md` の先頭に移す。

---

## このプロジェクトのファイルの使い分け

| ファイル | 中身 | いつ読むか |
|---|---|---|
| **課題.md**（このファイル） | 未対応・判断待ち・対応しないと決めたもの | **毎回** |
| **仕様.md** | 現在の仕様（中断条件A-1〜B-11、文言、寸法、不採用にした設計案） | **毎回**。改修の可否・影響範囲はここで判断する |
| **履歴.md** | バージョンごとの変更経緯と検証の記録 | 「なぜこうなっているか」を追うときだけ |
| **検証手順.md** | 検証環境の作り方とファズの不変条件 | 検証を依頼されたときだけ |

**改修・検証を頼まれたら、まず 課題.md と 仕様.md を読むこと。**
`履歴.md` は必要になった箇所だけを検索して読む（先頭から通読しない）。

---

## A. 対応する（未着手）

**現在なし。**

> ここには「やると決めたが、まだ手を付けていないもの」だけを書く。
> 新しく見つかった不具合は、まずこの欄に追記してから着手する。

---

## B. 判断待ち・未決

| # | 内容 | 論点 |
|---|---|---|
| B-a | メッセージ欄の高さ（`max-height:220px`） | 中断理由が複数行になったため、2件程度でスクロールが必要になる。広げるか、このままか |

---

## C. 対応しないと判断したもの

同じ指摘を繰り返さないための記録。**再提案するときは、判断の前提が変わったかどうかを先に確認する。**

| 内容 | 判断の前提 |
|---|---|
| 印刷であふれが**16名以上**になると日勤が2ページに分かれ、夜勤が3ページ目へ回る | その日の日勤が58名以上でないと起きず、実運用では起こりえない |
| 画面幅**1024px以下**で横スクロールが出る（必要幅 約1110px） | 利用環境は1366px以上 |
| 座席カードで、Courier New 環境では最長の時刻 `22:30※-32:00` がバッジ列に約7px重なる計算になる（仕様.md §9） | **利用環境はWindowsのみ**でConsolasが必ず入っている。Courier Newへフォールバックする環境で開く予定はない |

> 設計案そのものの不採用（「氏名照合を NameMap でラップする」など）は
> **仕様.md §7** にまとめてある。こちらは「既知だが直さない挙動」だけを扱う。

---

## D. 要請が出たら着手するもの

| 内容 | 着手の合図 | 対応方針 |
|---|---|---|
| **新人の座席指定** | 「この新人をこの席に」という要望が現場から出たとき | 優先フラグで代用させない。ojt.csv のように**対象座席を書ける形式**へ rookie.csv を変更する。マニュアルには「開発者に連絡してください」と案内済み（7章・12章） |
| **役席・GL採用の新人がOJT研修を受ける場合**（仕様.md §5） | そうした研修が実際に始まったとき | 現状、役席・GLとして出勤する日は rookie.csv も ojt.csv も効かない（新人固定席は座席1〜15のルール／OJT対象者が役席・GLの日は教官も座席側へ移らない）。別途の設計が要る |

---

## 追記のしかた

新しい案件は次の形で **A** に足す。

```
| A-n | 一行の要約 | 現象 → 原因の見当 → 直し方の候補 | 関係ファイル |
```

- 着手して直したら **A から消し、`履歴.md` の先頭へ**（ver番号・現象・原因・直した箇所）
- 仕様そのものが変わったら **`仕様.md` を上書き**する（履歴は残さない）
- 「直さない」と決めたら **C へ移し、判断の前提を書く**（前提が変われば再検討できるように）
