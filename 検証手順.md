# 検証手順

**検証を依頼されたときだけ読む。** 毎回この環境を組み直して再現できるようにするための記録。
検証の結果そのものは `履歴.md` の「検証の記録」に追記する。

---

## 環境の作り方

```
Node で csv.js / algorithm.js を直接 require（window をグローバルに用意）
jsdom で index.html を読み込み ui.js を eval（alert / confirm / prompt / Blob をフック）
Playwright + Chromium で index.html / manual.html / 印刷ページを実レンダリング
  → 要素の getBoundingClientRect、Range による文字の実インク位置、page.pdf のページ数
pypdf でPDFのページ数を取得
```

- `csv.js` / `algorithm.js` は純粋なロジックのため Node から直接叩ける
- `ui.js` は DOM を前提にするため jsdom が要る。ダイアログ系は必ずフックする
  （フックしないと確認ダイアログで止まる）
- **表示崩れは jsdom では検出できない。** 実寸が要るものは Playwright を使う
  （`.info` の固定幅とバッジの重なりは、これで初めて見つかった）

---

## ファズ検証の不変条件

3,000ケース程度をランダム生成して、次がすべて成り立つことを確認する。

| # | 不変条件 |
|---|---|
| 1 | 人数が保存されている（入力の出勤者数＝座席＋枠＋あふれの合計） |
| 2 | 二重配置がない（同じ人のカードが2枚できない） |
| 3 | 1座席につき2名まで |
| 4 | 禁止席に座っていない |
| 5 | 隣接禁止が守られている（**隣接・同席の両方**） |
| 6 | 勤務時間の重なる2人が同席していない |
| 7 | 座席9番が、名指しされていない人に使われていない |

**B-1〜B-11 で中断する入力は検査対象から除外する**（実際の画面では実行されないため）。

---

## 入り口を変えた検査（ver0.5.8で追加）

不変条件のファズだけでは「自動配置の結果」しか見ていない。入り口を変えると別の層の
不具合が出る。実際、下の3・6・7でそれぞれ不具合が見つかった。

| # | 検査 | 見るもの |
|---|---|---|
| 1 | **CSVパーサーの性質テスト** | ランダムな値を書き出して読み戻し、往復一致と行番号が実ファイルと合うこと |
| 2 | **手動操作ファズ** | ドラッグ・✎編集・削除・次案・シャッフルをランダムに混ぜ、人数・二重表示・例外を見る |
| 3 | **保存→読み込みファズ** | 盤面を座席・時刻・**バッジ**まで文字列化して完全一致を照合（バッジ消失・新人順位の入れ替わりはこれで発見） |
| 4 | **違反チェックをオラクルに使う** | 自動配置の直後に「手動変更後チェック」を押し、ルール違反が報告されないこと。`algorithm.js` と `ui.js` はルールを別実装しているので相互の突き合わせになる |
| 5 | **印刷HTMLの網羅性** | 盤面の全員がちょうど1回ずつ印刷に出ること |
| 6 | **候補の巡回** | 候補を一周させて候補1に戻ると盤面が一致すること（「候補1に戻す」で戻れない不具合はこれで発見） |
| 7 | **メタモルフィック検査** | 意味が変わらない書き換え（secret/rookie/月間シフトCSVの**行順**、氏名のスペース表記）で、中断の有無・中断理由・バッジが変わらないこと |
| 8 | **操作順カオス検査** | 未読み込みのまま実行／配置前に印刷／保存データを先に読む など、**手順を崩して**例外・二重表示・人数超過が出ないこと |
| 9 | **エスケープ** | 氏名に `<img src=x onerror=...>` `&` `"` を入れ、画面・印刷・保存/読み込みのどれでもタグとして解釈されないこと |
| 10 | **仕様と定数の突き合わせ** | 画面ヘルプ・マニュアルに書いた数値（隣接表・座席番号マップ・新人固定席の順・OJT既定順・夜勤/遅番の判定条件・候補上限・探索の制限時間）をコードから取り出して照合 |

**比較するときは、実行ごとにランダムに変わる部分を除く。** 夜勤GL枠に誰が入るかは
ランダムなので、枠のカードと座席のカードを同じ土俵で比べると誤検知になる。
隣接禁止のグループ記号（A/B/C）も「出てきた順」に振るため、行順を変えれば変わってよい。

---

## 見落としやすい観点

過去に不具合が出た箇所。新しい検証を組むときの手掛かり。

| 観点 | 過去の例 |
|---|---|
| **実寸のレイアウト** | 固定幅の要素からテキストがはみ出し、隣の要素の上に描画される（`.info` 93px） |
| **複数ファイルの同時読み込み・再入** | 後続のファイルの表示が先のエラーを消す／ダイアログがファイル数だけ出る |
| **月またぎ・年またぎのデータ** | 先頭の日付だけで年月を決めていて、1月分が12月として表示される |
| **行番号のずれ** | 空行を取り除いてから添字で行番号を作ると、実ファイルの行番号とずれる |
| **画面・保存・印刷の3経路** | 片方だけ直っていることがある。日付・タイトル・ファイル名まで揃えて確認する |
| **ファイルを直して読み込み直した後** | 前回のエラー表示が消えず、ファイル状態と食い違う |
| **処理順序に依存する不具合** | 先に処理されるルールが、後のルールの前提（`available` など）を壊す（B-6・B-11） |
| **利用者が書いていないルールのメッセージ** | ツールが内部で足す指定（夜勤の座席10）を、利用者向けの文言がそのまま名指しする |
| **同じ判定の二重実装** | 自動配置と違反チェック、✎編集とドラッグ、実行直後と保存読み込み後で、片方だけ更新される |
| **入力の行順への依存** | 行順を変えると中断の有無やバッジが変わる（重複判定のキーと集計キーの取り違えなど） |

---

## 依頼のしかた（テンプレ）

チャットで検証を頼むときは、この4行があれば足りる。

```
検証をお願いします。
対象：（例）style.css の表示崩れ／複数ファイル同時ドロップ
手順：docs/検証手順.md のとおり
結果は docs/履歴.md の「検証の記録」に追記する形でまとめてください。
```
