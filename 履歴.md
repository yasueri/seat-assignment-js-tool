# 履歴 — 変更と検証の記録

座席配置ツール。**追記のみ。過去の記述は書き換えない。**
現在の仕様は `仕様.md`、未対応の案件は `課題.md` にある。このファイルは
**「なぜこうなっているか」を追うときだけ**開く（先頭から通読しない）。

新しいバージョンは**先頭**に足す。

---

## 変更履歴

- 2026-08-02：**ver0.5.7.6。** 不具合チェック（ファズ400ケース＋シナリオ約40件）で見つかった
  点の修正（①〜⑤・⑧〜⑫）と、依頼2件（⑥⑦）、およびマニュアルの追随（⑬）。ファズの不変条件1〜7は違反ゼロだったため、
  配置ロジックそのものではなく、いずれもメッセージ・表示・ドキュメントの不整合。
  ①**夜勤の役席・GLが rookie.csv に載っていると、警告文が事実と逆だった**（`ui.js`）。
  夜勤の役席・GLは1名だけが夜勤GL枠に入り、**2人目以降は座席1〜15に並ぶ**ため
  新人固定席がそのまま効く（実際に座席5へ着席し「新人1」バッジも付く）。
  それなのに「新人固定席は効きません／早番・遅番エリアまたは夜勤GL枠に配置します」と
  出していた。日勤側・夜勤GL枠・夜勤の座席側の3通りに文言を分け、夜勤側は
  `assignNightLeaders` の戻り値（`glState['2-1']` / `seatLeaders`）が出てから出すようにした。
  あわせて、仕様.md §5 が書いていたのに実装に無かった
  **「対処は不要です。同じ人がOP勤務の日は新人固定席が効くため rookie.csv はそのままに」**
  の一文を追加した。
  ②**「夜勤の役席・GL2人目は座席10へ」が黙って通らないことがあった**（`ui.js`）。
  この指定は `ui.js` が `silent:true` で足す内部のもので、本人が rookie.csv に載っている・
  別の固定席を持っている・座席10を禁止席にしている・他の方が固定席10を持っている、の
  いずれかで通らなくなる。ver0.5.7.5までは**何も出ない**か、逆に
  「指定された座席に配置できません」という**利用者が書いていない固定席を指す**
  メッセージが出るかのどちらかだった（後者はsecret.csvを見ても直せない）。
  夜勤の配置後に座席10に入ったかを確かめ、入っていなければ**実際の行き先を添えて
  黄色で1件**出すようにした。中断はしない（本人の固定席が優先されただけ、という
  CSVの矛盾ではないケースを含み、直し方も一つに決まらないため。§1）。
  ③**silent 行を利用者向けメッセージから外した**（`algorithm.js`）。
  `stepDesignated` の「指定された座席に配置できません」は
  `designatedSeatsMapForBadge` に載っている人だけに出し、
  `detectStaticContradictions` のB・Cは silent 行だけの人を対象外にした。
  ver0.5.7.5で**バッジ**から silent を外したのと同じ理由を、メッセージにも広げたもの。
  仕様.md §8 に追記。
  ④**`detectStaticContradictions` が座席を内部キーで出していた**（`algorithm.js`）。
  「固定席指定（2-3）は…」「座席10番（2-3）だけを候補に…」のように、画面に無い
  `行-列` のキーがそのまま出ていた。`numberOfKey` を通して**座席番号**に統一した
  （同関数のCだけは元から番号に変換していたため、A・Bの変換漏れだった）。
  ⑤**印刷の「あふれ」欄に残業マーカーが出ていなかった**（`ui.js`）。
  時刻を素のテキストで書き出していたため、あふれた人の前残業・後残業（※／◆）が
  紙から落ちていた。座席カードと同じ `printTimeSpan` を通し、印刷CSSに
  `.print-overflow .pt.ot` を足し、凡例の集計（`collectOTKinds`）にもあふれ欄を含めた。
  ⑥**rookie.csv の新人度合いを整数のみに統一した**（`csv.js`。依頼）。
  `parseFloat` だったため小数が通り、`3級` のような数字混じりの文字列も 3 として
  通っていた。secret.csv の優先フラグ（`/^\d+$/`）に揃えて**0以上の整数のみ**とし、
  **半角・全角どちらの数字でもよい**点は据え置き（`normalizeDigits`）。
  順位付けにしか使わない値のため小数を認める理由がなく、「数値」の説明が
  マニュアル上で2種類に分かれるのを避けた。仕様.md §8 に書式の表を追加。
  ⑦**仕様.md §9（座席カードの寸法）を実物の `style.css` に合わせて書き直した。**
  §9は1マス224px・`.info` 124px と書いていたが、実際は**1マス202px・`.info` 105px・
  `gap` 10px・`.ot-time` padding `0 3px`・`.leader-area` 418px** で、
  §9が挙げる2案（224px／従来190px）のどちらとも違っていた。style.css を正として
  §9と課題.md Cを書き直した。Consolas では最長の時刻が約2pxの余裕で収まる。
  Courier New では約7px重なる計算になるが、**利用環境はWindowsのみ（Consolasが必ず入る）**
  のため対応しないと決め、課題.md C に前提つきで記録した。
  ⑧**配置に使える行が0件の月間シフトCSVを、読み込み不合格の扱いにそろえた**（`ui.js`）。
  ver0.5.7.5までは赤い本文だけを出しながら状態表示は「読み込み済み」で、直後に
  「月間シフトCSVを読み込みました。」という青い行まで並んでいた。ダイアログもスクロールも
  出ない（`endLoadBatch` は失敗0件だと出さない）ため、肝心のメッセージが画面の下に
  埋もれていた。`refreshShiftMonthly` は使える行があるかを返すだけにし、0件なら
  `rejectFile` を通す（A-1・A-2と同じ形。赤い本文＋行ごとの警告＋ダイアログ1回＋スクロール）。
  仕様.md §3 に追記。
  ⑨**1件も指定を作れなかった行を「同じ種別に複数行」と数えないようにした**（`csv.js`。B-1）。
  `固定席,甲,,99番,` のように座席番号がすべて無効な行でも、tokens を読む前に重複登録
  していたため中断していた。効いていない行を理由に止めるのはB-1の目的（後の行が黙って
  無視される）から外れる。その行には黄色の警告が別に出る。仕様.md §4 に追記。
  ⑩**ojt.csv の重複検出で、判定キーと集計キーの取り違えを直した**（`csv.js`。B-2・B-3）。
  判定は `nameKey`、集計は表示用の氏名という食い違いがあり、「山田太郎」と「山田 太郎」の
  ように表記だけ違う2行では、メッセージに2行目の表記しか出ないことがあった。
  `duplicateMentors` / `duplicateTrainees` のキーを `nameKey` にそろえ、表示には
  そのキーで最初に出てきた表記を使う。
  ⑪**新人候補の並び替えで人のコピーを作るのをやめた**（`algorithm.js`）。
  `{ ...byName.get(name), degree }` としたコピーを `stepRookies` がそのまま座席に置いており、
  `rookieRank` だけ実物にも代入していたため実害は出ていなかったが、以後 people に項目を
  足すと「座席にいる新人だけ古い内容」という事故になる。並び替え用の情報を別に持ち、
  座席に置くのは people の実物にした。**挙動は変わらない（予防的な整理）。**
  ⑫`findSharedSeatAnywhere` のコメントを訂正（`algorithm.js`）。「全15席から探す」と
  書いてあったが、`allowSeat9` を渡していないため座席9番は対象外。挙動が正しく、
  コメントが誤っていた。
  ⑬マニュアル（`manual.html`）を①②⑥に合わせて更新。7章の新人カードに夜勤の役席・GLの
  分岐を追記し、12章のメッセージ一覧に「夜勤の役席・GLとして座席1〜15に並ぶため新人固定席が
  そのまま効きます」「座席10へ入れようとしましたが…」の2行を追加、新人度合いの説明を
  整数に直した。なお**B-10（教官とOJT対象者の兼務）は12章に既に載っており、記載漏れは
  無かった**（チェック時の見落とし）。

- 2026-08-02：**ver0.5.7.5。** 依頼2件。
  ①**同日2回勤務の警告の文言を変更した**（`csv.js`）。
  「応援勤務でなければ入力をご確認ください。」
  →「間違いであれば、元のExcelを修正し、CSVを出力し直してください。」
  警告そのものの条件・レベル（`warn`）は変更していない。
  この行は読み飛ばさずに受け入れた行だが、直すには元のExcelを直して出力し直す必要が
  あるため、末尾は同じファイル内の読み飛ばし系メッセージと同じ形にそろえた
  （CSVを直接直しても次回の出力で戻るため）。
  ②**夜勤の役席・GLに固定席10を設定すると「固定席 10・10」と重複表示されていた**
  （`algorithm.js` / `ui.js`）。夜勤の役席・GLが2名以上の日、座席側へ回る先頭の1名は
  `ui.js` が `{ type:'seat_designated', silent:true }` の行を1行足して座席10へ
  強制配置している。`buildSecretIndexes` は `designatedNames`（バッジを出すか）からは
  silent行を除いていたが、`designatedSeatsMap`（バッジの座席番号の元）からは
  除いていなかったため、**secret.csv で座席10を固定席に指定した本人がそのまま
  座席10へ回ると、同じ番号が2件入って「10・10」になっていた。**
  バッジ表示専用の `designatedSeatsMapForBadge`（silent行を除く）を追加し、
  `designatedSeatNumbers` の生成をこちらへ移した（`ui.js` の `deriveBadgeFields` も同じ
  マップへそろえた）。配置本体と `detectStaticContradictions` は従来どおり
  silent行を含む `designatedSeatsMap` を使う（座席10への強制配置と、その座席が
  本人の禁止席と矛盾していないかの検知は、内部指定も対象にする必要があるため）。
  仕様.md §8 に「バッジは secret.csv の記載だけで決まる（silent 行）」を追加。
  なお **secret.csv 側の重複（同じ行に `10 10`、同じ人に固定席が2行）は元から
  塞がっている**（行内は `seenNumsInRow`、行またぎはB-1で中断）。今回の重複の原因は
  内部指定との重なりだけだった。
- 2026-08-01：**ver0.5.7.4。** ver0.5.7.3 のコードレビューで見つかった5点を修正。
  ①**同じ人が「教官」と「OJT対象者」の両方に書かれていると二重配置になっていた**（B-10）。
  同じ行の中で教官名＝OJT対象者の場合は ver0.5.3 から読み飛ばしていたが、行をまたぐ場合は
  素通りしていた。その人は教官としての同席で1席、OJT対象者としての同席でもう1席を占め、
  同じ方のカードが座席表に2枚できる（ver0.4.0で廃止したはずの二重配置）。警告も出ない。
  `parseOjtRows` が `mentorTraineeConflicts` を返し、`collectBlockingProblems` で中断する。
  ②**教官・OJT対象者どうしの隣接禁止が黙って破られていた**（§5）。`findOjtPairInOrder` が
  同席する3人の間の隣接禁止を照合していなかった。配置先が構造的に隣接席に固定されている
  ため探し直しでは解決せず、**オレンジ（violation）で知らせる**扱いにした。
  教官×OJT一人目のときだけは同じ座席になるため、同席をやめて個別配置に切り替える。
  ③**ojt.csv の教官が rookie.csv にも載っていると、出勤しているのに「不在」扱いになっていた**
  （B-11）。新人固定席は教官・OJTより先に処理されるため、教官が先に着席し
  `available` が false になる。**B-6（教官に優先フラグ）とまったく同じ機序**なので、
  中断条件も文言もB-6に揃えた。あわせて §4「対象に含めないもの」の
  「新人 × 教官も同じ理由で対象外」という記述を撤回した（下記）。
  ④**教官とOJT対象者が日勤側・夜勤側に分かれる日に、黄色でお知らせする**ようにした（§5）。
  日勤と夜勤は座席表が別物のため同席できず、実際には反対側の別の教官へ自動的に
  振り分けられる。運用上、夜勤勤務予定の方がOJTだけ日中に行うことがよくあるが、
  その日は対象者も日勤側に来るためこの分岐には入らない。**対象者が夜勤側に来ている＝
  勤務時間そのものが夜勤へ移っている**ということなので、ojt.csv の更新忘れの可能性が高い。
  ただし「その日は振り分け先の教官で回す」判断もあり得るため中断はしない。
  あわせて `algorithm.js` の「本日不在」という表現を「この座席表にいない」へ変更した
  （反対側に出勤している場合もこのメッセージが出るため、事実と食い違っていた）。
  ⑤**隣接禁止のペアが「同じ座席」の1人目・2人目に入れてしまっていた**。`ADJACENCY` は
  自分の席を含まないため、`canPlace` も手動チェックも同じ机の共有を見ていなかった。
  隣接禁止は勤務時間の重なりに関係なく常に適用する方針なのに、隣の席はだめで同じ机なら
  よいという状態だった。`canPlace` / `canPlaceSharedSeat` / `checkPlacementViolations` の
  3か所を修正（全探索・貪欲・一部シャッフルはすべて `canPlace` を通るため全経路に反映）。
  ⑥**残業マーカーの付いた時刻が、座席カードのバッジに重なっていた**（style.css）。
  `.person-card .info` が `width:93px; flex-shrink:0` の固定幅で、時刻行は `nowrap` かつ
  `overflow` の指定がないため、はみ出した文字がバッジの上に描画されていた。
  座席1マス 190px → **224px**、`.info` 93px → **124px** ほかを調整した（§9）
- 2026-07-31（3）：**ver0.5.7.3。** ver0.5.7.2 のコードレビューで見つかった3点を修正。
  ①**メッセージの「◯行目」が実際のファイルの行番号とずれていた**。`parseCSV` が空行を
  取り除いてから各パーサーが「データ行の添字＋2」で行番号を作っていたため、空行より
  下の行がすべて1つ手前の番号で表示されていた。B-8・B-9の中断メッセージは行番号だけが
  直す手がかりのため、`parseCSV` が各行に `lineNo`（ファイル内の実際の行番号）を持たせ、
  4つの解析関数はこれを使う。セル内改行があっても行の先頭を指す。
  ②**secret.csv だけ完全な空行を弾いていなかった**。Excelで行の中身だけを消すと
  `,,,,` が出力され、その行数だけ「種別「」は認識できません」が並んでいた。
  ojt.csv・rookie.csv と同じ空行ガードを追加（マニュアル10章の「空行＝読み飛ばされます」に
  実装を合わせたもの）。
  ③**読み込みを弾いたファイルを直して読み込み直しても、赤いエラーが消えなかった**。
  `endLoadBatch` が「出すものが1件でもあるとき」しかメッセージ欄を描き直さず、初回の
  自動配置より前は読み込み成功時にログが1件も出ないため、前回の赤が残り続けていた。
  ファイル状態は「読み込み済み」なのにメッセージ欄は赤のままという食い違いになる。
  読み込めたファイル名を必ず1行出す（`loadedLabels`）ようにして打ち消す。あわせて
  読み込み処理の多重実行に備え、まとめ表示に深さ（`loadBatchDepth`）と try/finally を入れた。
  なおB-6のOJT対象者側のメッセージは、担当教官が本日不在の場合に本来の担当教官名を
  出していて読み手が混乱するため、出勤している場合としていない場合で書き分けた。
  あわせて画面の使い勝手を2点調整した。**読み込み後にメッセージ欄へ自動で移動するのは、
  ダイアログを出すとき（ヘッダー不一致・文字化け・ファイル名不明）だけにした。**
  読み込み時に行うのは見出しと文字コードの確認だけで中身の解析は「自動配置を実行」まで
  行わないため、全部読み込めたときにメッセージ欄で読むものは無い。一方で次の操作
  （配置対象日の選択・自動配置を実行・追加のファイルの読み込み）はすべて
  「1. CSV読み込み」の中にあり、下へ飛ばすと必ず戻る操作が要っていた。
  実行・違反チェック・保存データの読み込みでの移動は従来どおり。
  **メッセージ欄の色の凡例は既定で畳んだ**（「色の意味（赤・オレンジ・黄・青）」の
  トグル）。毎回同じ説明が場所を取り、実際のメッセージが下へ押し下げられていたため
- 2026-07-31（2）：**ver0.5.7.2。** ver0.5.7.1 のコードレビューで見つかった4点を修正。
  ①**任意CSVの未読み込みメッセージから時制を外した**（「使用せず配置しました」→
  「使用しません」）。B-1〜B-9で中断して盤面が空のときも、このお知らせだけが
  過去形で残り「配置した」と読めていた。
  ②**読み込みを弾いた任意CSVのまま実行しようとした場合、実行前に確認を出す**（§3）。
  弾かれたファイルは `rawText` が null になるため「はじめから渡していない」場合と
  区別が付かず、青のお知らせ1行でルールの効かない座席表が完成していた。
  `rejectedFiles` で弾いた記録を保持し、実行前に OK／キャンセルを出す。
  メッセージ欄の文言も未提出（青）と読み込み失敗（黄）で出し分ける。
  月間シフトCSVが弾かれた場合は「選択してください」ではなく
  「読み込めていないため実行できません」と出す。
  ③**年末年始の月またぎCSVで日付表示が誤っていた**（§8）。12月＋翌1月上旬が
  1つのCSVに入るため、先頭の日付だけで年月を決めていた従来の実装では、
  1月を選んでも「2025年12月」と表示され、プルダウンも「31日（水）／1日（木）」と
  12月の続きに見えていた。年月表示を範囲（`2025年12月〜2026年1月`）にし、
  2つ目以降の月の日付には全件「1月1日（木）」と月を付ける。単月のCSVでは
  従来どおりの見た目。
  ④固定席・要サポートの違反メッセージにだけ **【日勤】【夜勤】が付いていなかった**ため
  付加し、あわせて「現在は11番の座席」「現在はあふれ欄」と現在地を添える。
  指定席にいないことだけでは、座席表とあふれ欄を目で探す手間が残るため
- 2026-07-31：**ver0.5.7.1。** ver0.5.7 のコードレビューで見つかった5点を修正。
  ①**B-9の対象を拡張**（§4）。種別が正しくても対象座席などが読み取れず行ごと
  読み飛ばした場合も、残るのは優先フラグだけで結果は同じため中断する。
  ②**優先フラグの値が数値でないときは弾く**（従来どおり）と明文化し、あわせて
  種別の警告を抑止する条件を「優先フラグ欄に何か書いてある」から
  「優先フラグを実際に登録できた」に変更（§4のB-9）。
  ③**やり直しの確認ダイアログを中断判定より前に移動**（§2）。中断が起きるときだけ
  確認なしに盤面が消えていた。
  ④**読み込み結果をまとめて1回だけ表示**し、読み込み時のダイアログも
  「件数とファイル名だけ」に統一（§3）。
  ⑤B-4の文言から「の両方に」を削除（§6）。3つ以上当たると日本語が崩れるため。
  現状維持と決めたものは §7 に記録した
- 2026-07-30（4）：**ver0.5.7 として全件実装。** 実装時に決めたこと3点。
  ①B-8で**日付そのものが読めなかった行は、選択日を問わず中断**する
  （どの日の勤務か判別できないため。「出勤者のみ対象」の唯一の例外）。
  ②B-4〜B-7・B-9の対象を「座席1〜15に並ぶ人」へ絞る案を検討したが**撤回**（理由は§7）。
  対象は**選択日の出勤者全員**。B-6のみ教官・OJTの処理が動く日に限る。
  ③B-5は**部分重複でも中断**する（固定席3席のうち1席だけが禁止席と重なる場合も）。
  あわせて§8の「氏名照合のスペース吸収（未着手）」を訂正（ver0.5.6で実装済みだった）
- 2026-07-30（3）：プロジェクトの実装と突き合わせて4か所を訂正。
  ①CSV解析の引用符対応は**すでに実装済み**（作り直し不要）。②A-1は**すでに前方一致**で
  判定しており、必要なのは警告→中断の格上げのみ。③rookie.csvの人が役席・GLで出勤する
  ケースは ver0.5.6 で中断を実装したが、**警告へ戻す**（理由は§5）。④B-9の「要確認」を
  解消（優先フラグは種別が空欄でも実際に効いている）。
  あわせてB-4の言い方を「座る席を指名するルール」に統一し、夜勤GL席は対象外と決定
- 2026-07-30（2）：B-9（優先フラグ単独）を新設。
  B-4は例外なし（新人×固定席／要サポートは優先フラグがあっても中断）。
  新人の座席指定は開発者への連絡に一本化し、rookie.csvは要請が出るまで変更しない
- 2026-07-30（1）：新人×禁止席／隣接禁止＋優先フラグは許容で確定（未決なし）


---

## 完了した付随作業（マニュアル・readme）

以下はすべて反映済み。**現在のマニュアルの内容そのものではなく、いつ何を直したかの記録。**

### マニュアル（manual.html）の修正 — ver0.5.7.4で完了

以下を反映済み。

- **12章 メッセージ一覧（赤）**に B-10・B-11 の2件を追加
- **12章**に、教官・OJT対象者どうしの隣接禁止（オレンジ）と、
  教官とOJT対象者が日勤側・夜勤側に分かれる日（黄）の2件を追加
- **7章「隣接禁止」**の「隣」の定義に**同じ座席（1人目と2人目）を含む**ことを明記
  （index.html の「CSVの形式について」も同じ記述のため、あわせて修正済み）
- **7章 OJTカードのつまずき**に4件追加（教官とOJT対象者の兼務／教官を rookie.csv に載せる／
  教官・OJT間の隣接禁止／日勤側・夜勤側に分かれる日）
- 「担当教官が**本日不在**」という表現は manual.html には無かったため修正不要だった

### readme.txt の修正 — ver0.5.7.4で完了

先頭のバージョン表記を `ver0.5.7.4` に更新した。

### マニュアル（manual.html）の修正 — ver0.5.7.1で追加

- 7章の優先フラグカード（書きかた・つまずき）と10章の共通ルール表を、
  「種別と併記」から「**同じ行に種別と対象座席を正しく書く**」に変更（B-9の拡張に対応）。
  理由は書かず、ルールだけを簡潔に示す
- 12章の中断表：B-9の行をメッセージの変更に合わせ、B-4の行から「の両方に」を削除
- 11章「同じ人が同じ日に2回出勤する場合」：時間帯が重なる場合の説明が
  「エラーを表示します」のままだったため、「その行を読み飛ばし、**配置対象日に選んだ日の
  行であれば配置を中断する**」に修正（4段階の色分けを入れて「エラー」の意味が
  「赤＝中断」に固定されたため）

### readme.txt の修正 — ver0.5.7.1

ツリー図の行頭が1か所だけ全角スペースでずれていたのを修正。`sample-csv` を配布しないため
「sample-csvの氏名について」の節を削除。バージョン表記を更新。


### マニュアル（manual.html）の修正 — ver0.5.7で完了

- **「入力エラーがあっても自動配置は止まりません」を書き換え**（1章⑤の注意書き、
  1章冒頭の流れ図、12章の冒頭）。赤なら座席表はできていない／それ以外は座席表を見て確認、
  の2段階で説明する
- **12章に「メッセージの4段階」と「自動配置を中断するとき（赤）」を新設。**
  中断条件A-1〜B-9をメッセージ・原因・対処の表にまとめた。既存の
  「CSVが反映されないとき」は黄（続行）専用の表に整理し、中断へ移った行を削除した
- 12章の対処列を、**Excelを直して再出力**する前提に修正
- **「座る席を指名するルールは、1人に1つだけ」を追記**（12章の中断表の直後、10章、
  index.htmlの処理順序の説明）。あわせて**禁止席・隣接禁止は一緒に使える**ことを記載
  （メッセージには書かないため、マニュアルが唯一の説明箇所）
- **「新人の座席を指定したい場合は、開発者に連絡してください」**を追記
  （7章の新人カード・優先フラグカード、12章の中断表）
- **優先フラグは種別と併記が必須**（7章の優先フラグカード、10章の共通ルール表、
  index.htmlのバッジ説明の3か所）
- **必要な列より右に自分用の列を足してもよい**（9章の早見表、10章、index.html）
- 姓名間のスペースに関する記述（10章・12章に既存。index.htmlの共通ルールにも追記）
- 副産物：7章の禁止席カードにあった `</b>` の閉じタグ漏れ1件を修正（ver0.5.6以前からの不具合）



---

## 検証の記録

検証環境の作り方は `検証手順.md` にある。

### ver0.5.7.4 — Chromium実レンダリング＋ファズ3,000ケース（2026-08-01）

前回のチャットで未着手だった5項目を検証した。ファズ検証（3,000ケース）は問題0件。

| # | 検証項目 | 結果 |
|---|---|---|
| 1 | manual.html と実装の整合 | **不整合なし**（定数・配置順・ペア候補表・保存対象すべて一致） |
| 2 | style.css の表示崩れ | **不具合1件 → 修正済み**（残業マーカー付きの時刻がバッジに重なっていた） |
| 3 | 印刷HTMLの実レイアウト | **問題なし**（あふれ16名以上で3ページになるが実運用では起こりえない） |
| 4 | 複数ファイル同時ドロップ中の再入 | **問題なし** |
| 5 | 年末年始の月またぎCSV | **問題なし** |
| 6 | 副次的に発見：幅1024px以下で横スクロール | 利用環境は1366px以上のため対応不要 |

**1. manual.html との照合** — 機械的に照合した項目はすべて一致。
新人固定席の順（`ROOKIE_DEFAULT_SEAT_ORDER`）、教官・OJT単独時の順（`OJT_DEFAULT_SINGLE_ORDER`）、
ペア候補の主要6組・予備6組（`OJT_PAIR_*` / `OJT_FALLBACK_PAIR_TAIL`）、夜勤・遅番の判定閾値、
全探索の制限5秒（`EXHAUSTIVE_TIME_BUDGET_MS`）、候補上限99（`EXHAUSTIVE_MAX_SOLUTIONS`）、
早番・遅番エリア6名（`fillLeaderArea`）、保存されない情報（`exportPerson` のホワイトリスト）。
内部リンク（`#...`）の破損0件、index.html → manual.html のリンク12個もすべて実在するアンカーを指す。
manual.html にJSエラー・横スクロールなし。

**4. 複数ファイル同時ドロップ** — ver0.5.7.3 の `loadBatchDepth` と try/finally が意図どおり機能。

| 検証 | 結果 |
|---|---|
| 4ファイル同時ドロップ（secret.csv だけヘッダー不正） | 赤1件＋読み込み成功3件を同時表示。**ダイアログは1回だけ** |
| 読み込み中に別ファイルを重ねてドロップ | **両方のメッセージが残る**（先に終わったほうが消してしまう現象は起きない） |
| 種類を判別できないファイルの混在 | 警告＋ダイアログが正しく出る |

**5. 年末年始の月またぎCSV** — 2025-12-28〜2026-01-05 を1ファイルにして確認。
年月ラベル `2025年12月〜2026年1月`、12月分のプルダウンは月なし、1月分は全件に月が付く。
1月を選んでの実行・保存ファイル名（`座席配置_20260101_…`）・保存データの `currentDate`・
印刷タイトル・12/31への選び直しまで、画面／保存／印刷の全経路で正しい。
