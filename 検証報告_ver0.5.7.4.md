# 検証報告 ver0.5.7.4（未着手だった範囲）

前回のチャットで未着手だった5項目を検証した。
検証環境：Chromium（Playwright）で **index.html / manual.html / 印刷ページを実際にレンダリング**し、
要素の実寸・テキストの実インク位置・PDFのページ数を測定した。
あわせて Node 上のファズ検証（3,000ケース）も再実行し、問題0件を確認している。

---

## 結果まとめ

| # | 検証項目 | 結果 |
|---|---|---|
| 1 | manual.html と実装の整合 | **不整合なし**（定数・配置順・ペア候補表・保存対象すべて一致）。ver0.5.7.4の追記は本チャットで反映済み |
| 2 | style.css の表示崩れ | **不具合1件 → 修正済み**（残業マーカー付きの時刻がバッジに重なっていた） |
| 3 | 印刷HTMLの実レイアウト | **問題なし**（あふれ16名以上で3ページになるが、実運用では起こりえないため対応不要） |
| 4 | 複数ファイル同時ドロップ中の再入 | **問題なし** |
| 5 | 年末年始の月またぎCSV | **問題なし** |

---

## 1. manual.html と実装の整合 — 不整合なし

機械的に照合した項目はすべて一致していた。

| 照合項目 | マニュアルの記述 | 実装 |
|---|---|---|
| 新人固定席の順 | 5→10→6→11→7→12→8 | `ROOKIE_DEFAULT_SEAT_ORDER` 一致 |
| 教官・OJT単独時の順 | 15→12→8→4→3→2→1 | `OJT_DEFAULT_SINGLE_ORDER` 一致 |
| ペア候補（主要6組） | 12/8・12/15・15/14・4/3・3/2・2/1 | `OJT_PAIR_*` 一致 |
| ペア候補（予備6組） | 12/11・8/7・11/10・7/6・6/5・14/13 | `OJT_FALLBACK_PAIR_TAIL` 一致 |
| 夜勤の判定 | 開始22:00より後／終了26:00より後 | `NIGHT_*_THRESHOLD` 一致 |
| 遅番の判定 | 12:00以降／前残業ありで10:00以降 | `LATE_*_THRESHOLD` 一致 |
| 全探索の制限時間 | 5秒 | `EXHAUSTIVE_TIME_BUDGET_MS=5000` 一致 |
| 候補の上限 | 99通り | `EXHAUSTIVE_MAX_SOLUTIONS=99` 一致 |
| 早番・遅番エリア | 6名まで | `fillLeaderArea` 一致 |
| 保存されない情報 | 固定席・禁止席・要サポート・隣禁止・優先・夜勤GL席 | `exportPerson` のホワイトリスト一致 |

その他の確認：

- **内部リンク（`#...`）はすべて有効**。壊れたアンカーは0件
- **index.html → manual.html のリンク12個も、すべて実在するアンカーを指している**
- manual.html に JSエラー・横スクロールなし

### ver0.5.7.4の追記（本チャットで反映済み）

- 12章 赤の表に **B-10・B-11** の2行
- 12章 黄の表に **日勤側・夜勤側に分かれる日** の1行
- 12章 オレンジの表に **教官・OJT間の隣接禁止** の1行
- 7章「隣接禁止」の定義に **同じ座席の1人目と2人目も隣に含む** ことを明記
  （index.html「CSVの形式について」も同じ文言を追加）
- 7章 OJTカードのつまずきに4件追加

---

## 2. style.css — 残業マーカー付きの時刻がバッジに重なっていた（**修正済み**）

### 症状

座席カードの氏名・時刻を入れる `.info` は **`width:93px; flex-shrink:0`** の固定幅で、
時刻行は `white-space:nowrap` かつ **`overflow` の指定がない**。
そのため時刻の文字列が93pxを超えると、右隣のバッジ列の上に**はみ出して重なる**。

### 必要幅の算定（残業は前・後のどちらか一方のみ）

Chromium で時刻テキストの**実インク幅**を測定した。

| 表示例 | Consolas（Windows既定） | Courier New（Consolasが無い環境） |
|---|---|---|
| `8:00-17:00` | 87px | 96px |
| `10:00-19:00` / `22:30-32:00` | 96px | 105px |
| `9:00※-18:00`（残業1つ） | 104px | 113px |
| **`22:30※-32:00`（最長）** | **113px** | **122px** |

残業が前後どちらか一方しか付かないため、最長は
**「時・分とも2桁 ＋ 残業マーカー1つ」の `22:30※-32:00`** となる。
なお `.info` 93px では、**残業マーカーが無くても `10:00-19:00`（96px）の時点で既に超えていた。**

### 適用した寸法

| 項目 | 変更前 | 変更後 | 根拠 |
|---|---|---|---|
| `.seat-grid` の1マス | 190px | **224px** | 下の内訳の合計 |
| `.person-card .info` | 93px | **124px** | 最長122px（Courier New）＋余裕2px |
| `.person-card` の `gap` | 10px | **8px** | 時刻の幅を優先 |
| `.ot-time` の `padding` | 0 3px | **0 2px** | 時刻行を2px詰める |
| `.leader-area` | 394px | **462px** | 座席グリッドと横幅をそろえる |

座席1マス224pxの内訳:

```
224 = 18(.seat padding) + 2(.seat border) + 3(.slot border)
    + 16(.person-card padding) + 3(.person-card border)
    + 124(.info) + 8(gap) + 49(バッジ列の最大幅＝3文字バッジ)
    = 223 → 切り上げて 224
```

**Windows の Consolas だけでなく Courier New でも収まる幅にした。**
Windows以外で開いたときに同じ不具合が再発しないようにするためで、
差は8px（座席グリッド全体で32px）にすぎない。

### 修正後の実測

| 表示例 | バッジとの余裕（Consolas相当） | （Courier New相当） |
|---|---|---|
| `22:30※-32:00` ＋ 禁止席バッジ | **20.8px** | **11.8px** |
| `10:00-19:00※` ＋ 禁止席バッジ | 20.8px | 11.8px |
| `9:00※-18:00` ＋ 優先＋固定席バッジ | 29.6px | 21.4px |

カード内のすべての要素についてはみ出し0件、氏名の省略も発生していない。
早番・遅番カードにも余裕がある（33〜43px）。
早番・遅番エリアと座席グリッドの横幅、候補欄とあふれ欄の左端も一致している。

### 副作用

- 座席グリッドの幅が 802px → **938px**、画面の必要幅が 約1062px → **約1198px**。
  利用環境は1366px以上のため影響しない
- **印刷は影響を受けない**（`buildPrintHtml` はmm指定の独立したCSS）。
  修正の前後でPDFのページ数・レイアウトが変わらないことを確認済み

### 従来幅（190px）のまま使う場合に残る重なり

幅を広げない選択もできるよう、**原本と完全に同一の `style_従来幅190px.css` も併せて出力**した。
そのとき残る重なりは次のとおり（Windows既定のConsolas、残業マーカーは1つ）。

| 表示例 | バッジとの重なり | 見え方 |
|---|---|---|
| `8:00-17:00`・`9:30-18:30`（開始が1桁時・残業なし） | なし | 問題なし |
| `10:00-19:00`・`22:30-32:00`（開始が2桁時・残業なし） | なし | 問題なし |
| `9:00※-18:00`（開始が1桁時＋残業） | 1.4px | ほぼ分からない |
| **`22:30※-32:00`・`10:00-19:00※`（開始が2桁時＋残業）** | **10.2px** | バッジの左端に時刻の末尾が数px重なる |

**重なるのは「開始が2桁時（10時台以降・夜勤）＋残業マーカー＋バッジ持ち」の組み合わせだけ**で、
量も約10pxにとどまる。これが従来幅で問題に気づかなかった理由と考えられる。

なお **190pxのままで重なりを完全に消すことはできない。**
カード内で使える幅は149pxで、バッジ列（3文字バッジで49px）を差し引くと
時刻に回せるのは100pxまでだが、最長の時刻は113pxを要するため
（時刻のフォントを16px→13pxまで縮めれば収まるが、紙で一目で読む用途には小さすぎる）。

`.ot-time` の `padding` を `0 3px` → `0 2px` にする**1行だけの変更**でも、
重なりは 10.2px → 6.2px に減る。幅を変えたくない場合の緩和策として使える。

## 3. 印刷HTMLの実レイアウト — 通常運用では問題なし

Chromium で実際にA4のPDFを生成し、ページ数を数えた。

| 状況 | あふれ人数 | PDFページ数 |
|---|---|---|
| 通常 20名 | 0名 | 2ページ（1枚目=日勤／2枚目=夜勤）OK |
| 満席 30名 | 10名 | 2ページ OK |
| 大人数 | **16名** | **3ページ** |

**あふれ15名までは1ページに収まる。16名を超えると日勤が2ページに分かれ、
夜勤が3ページ目へ押し出される。**
`.print-page + .print-page { break-before: page }` により夜勤は必ず改ページするため、
**「1枚目＝日勤／2枚目＝夜勤」の両面コピーが崩れる。**

### 影響の見積もり

座席は最大30名（15席×2）、早番・遅番エリアが12名なので、
あふれが16名出るのは**その日の日勤が58名以上**の場合。実運用では起こらない。
**手動で大量のカードをあふれ欄へ送った場合**にのみ起こりうる。

対処するなら、あふれ一覧の `grid-template-columns` を3列→4列にする（16名→20名まで収まる）か、
一定数を超えたら3ページ目に回す旨をマニュアルに書く。**優先度は低い。**

### あわせて確認したこと

- 凡例はページごとに出し分けられている（日勤に◆がなければ「※…OP残業」だけになる）— 正常
- 座席30枠（15席×2ページ）がすべて描画される — 正常
- 月またぎCSVでも印刷タイトルの日付が正しい — 正常

---

## 4. 複数ファイル同時ドロップ中の再入 — 問題なし

| 検証 | 結果 |
|---|---|
| 4ファイル同時ドロップ（secret.csv だけヘッダー不正） | 赤1件＋「月間シフトCSV / rookie.csv / ojt.csv を読み込みました」を同時表示。**ダイアログは1回だけ** |
| 読み込み中に別ファイルを重ねてドロップ（遅いファイル＋速いファイル） | **両方のメッセージが残る。**先に終わったほうがメッセージを消してしまう現象は起きない |
| 種類を判別できないファイルの混在 | 警告＋ダイアログが正しく出る |

ver0.5.7.3 で入れた `loadBatchDepth` と `try/finally` が意図どおり機能している。
ファイル状態（「読み込み済み」／「読み込めませんでした」）とメッセージ欄の内容も一致していた。

---

## 5. 年末年始の月またぎCSV — 問題なし

2025-12-28〜2026-01-05 を1ファイルにしたCSVで確認。

| 確認項目 | 結果 |
|---|---|
| 年月ラベル | `2025年12月〜2026年1月` |
| プルダウン（12月分） | `28日（日）` … `31日（水）`（月なし） |
| プルダウン（1月分） | `1月1日（木）` … `1月5日（月）`（**全件に月が付く**） |
| 1月を選んで実行 | 見出し `（2026年1月1日（木））`・配置とも正しい |
| 保存ファイル名 | `座席配置_20260101_保存日時_…json` |
| 保存データの日付 | `currentDate: 2026-01-01` / label も正しい |
| 印刷タイトル | `2026年1月1日（木） 日勤 座席表` / 夜勤も同様 |
| 12/31を選び直し | 見出し・配置とも正しく切り替わる |

ver0.5.7.2 の対応が全経路（画面・保存・印刷）で効いている。

---

## 6. 副次的に見つかったこと：幅1024px以下で横スクロール

固定幅レイアウトのため、一定の幅がないと横スクロールが出る。
項目2の修正で座席1マスを広げたため、必要幅は **1062px → 約1198px** に増えた。

| ブラウザ幅 | 修正前 | 修正後 |
|---|---|---|
| 1920 / 1440 / 1366 / 1280px | 問題なし | **問題なし** |
| 1200px | 問題なし | 候補欄とあふれ欄の左端がずれる（スクロールは出ない） |
| 1024px | 横スクロール | 横スクロール |

**利用環境は1366px以上のため影響しない**（対応不要と判断）。

---

## 検証に使った環境（次回の再構築用）

```
Node で csv.js / algorithm.js を直接 require（window をグローバルに用意）
jsdom で index.html を読み込み ui.js を eval（alert/confirm/prompt/Blob をフック）
Playwright + Chromium で index.html / manual.html / 印刷ページを実レンダリング
  → 要素の getBoundingClientRect、Range による文字の実インク位置、page.pdf のページ数
pypdf でPDFのページ数を取得
```

ファズ検証の不変条件：人数の保存／二重配置なし／1座席2名まで／禁止席／
隣接禁止（隣接・同席の両方）／時間重複同席／座席9番。
B-1〜B-11で中断する入力は検査対象から除外している（実際の画面では実行されないため）。
