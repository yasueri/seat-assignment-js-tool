# 実装依頼：氏名照合のスペース吸収

座席配置ツール ver0.5.6 に対する実装依頼。

**この依頼は、以前に検討した `NameMap` / `NameSet` でラップする案を置き換えるものです。**
そちらの案は採用しません（理由は §6）。

---

## 1. やりたいこと

CSVをまたいで氏名を突き合わせるとき、**姓名の間のスペースの違いを無視して同じ人と見なす。**

| 月間シフトCSV | secret.csv などの設定ファイル | 現在 | 変更後 |
|---|---|---|---|
| 山田 太郎（半角） | 山田太郎（なし） | 別人 | 同じ人 |
| 山田 太郎（半角） | 山田　太郎（全角） | 別人 | 同じ人 |
| 山田太郎（なし） | 山田 太郎（半角） | 別人 | 同じ人 |

現在は文字列が1文字でも違えば別人になるため、固定席・新人固定席・OJT・要サポート・禁止席・
隣接禁止・優先フラグのすべてが**黙って効かなくなる。**
座席表を見ても普通に座っているだけなので気づけない。これが運用上いちばん多いミス。

### 対象外（今回はやらない）

- 全角英数字と半角英数字の同一視
- 旧字体・異体字の同一視（髙／高 など）
- 氏名の打ち間違いの検出

---

## 2. 方針：入口で氏名を書き換える

照合箇所は約60か所（Set / Map への投入と照合が54、`===` 比較が6）ある。
そこを1つずつ直すのではなく、**CSVを読む時点で氏名を「月間シフトCSVの表記」に寄せてしまう。**
そうすれば下流（`ui.js` / `algorithm.js` / `pkey`）は**一切変更不要**になる。

### 2つの関数を用意する

```js
// 照合用のキー。空白をすべて取り除く。
// JSの \s は全角スペース(U+3000)・ノーブレークスペース(U+00A0)・タブ・改行を含む。
function nameKey(s) {
  return String(s == null ? '' : s).replace(/\s+/g, '');
}

// 表示用の氏名。連続する空白（改行・タブ・全角スペースを含む）を
// 半角スペース1つに詰め、前後は削る。
function displayName(s) {
  return String(s == null ? '' : s).replace(/\s+/g, ' ').trim();
}
```

`displayName` は画面・印刷に出る氏名を揃えるためのもの。
**VBAの出力（ExportShiftCSV_v6）は氏名を正規化前の生の値で書き出している**
（`CSVEscape(SanitizeCell(staffName))`。`NormalizeName` を通した `normName` ではない）。
そのため元Excelの氏名セルに改行・タブ・二重スペースが入っていると、そのままCSVに出て
座席カードのレイアウトが崩れる。ここで吸収する。

### 手順

1. **月間シフトCSVを読むとき**、氏名に `displayName` を適用する。
   同時に `nameKey(氏名) -> displayName(氏名)` の対応表を作る（同じキーは初出を採用）。
   これが「その月の正しい表記」になる。
2. **secret.csv / ojt.csv / rookie.csv を読むとき**、氏名セルごとに対応表を引く。
   - 見つかった → **月間シフトCSVの表記に置き換える**
   - 見つからない → `displayName` を適用した文字列のまま（休職者など。従来どおり静かに効かない）
3. これで下流の氏名はすべて同じ表記になり、既存の照合はそのまま通る。

---

## 3. 変更箇所

### csv.js

| 関数 | 変更内容 |
|---|---|
| （新規） | `nameKey` / `displayName` を追加し、`window.SeatTool.csv` から公開する |
| `parseShiftMonthlyRows` | 氏名に `displayName` を適用。戻り値に `nameMap`（`nameKey -> 表示名` の Map）を追加する。`pkey` は書き換え後の氏名で作る |
| `parseRookieRows` | 第2引数に `resolveName` を受け取り、氏名セルに適用する |
| `parseSecretRows` | 第3引数に `resolveName` を受け取り、**対象1・対象2**の両方に適用する |
| `parseOjtRows` | 第3引数に `resolveName` を受け取り、**教官名・OJT一人目・OJT二人目**すべてに適用する |

`resolveName` は省略可とし、渡されない場合は `displayName` と同じ動作にする
（引数を増やしても既存の呼び出しが壊れないようにするため）。

```js
// 呼び出し側で作る関数のイメージ
function makeResolveName(nameMap) {
  return function (raw) {
    const disp = displayName(raw);
    if (!disp) return '';
    const hit = nameMap && nameMap.get(nameKey(disp));
    return hit || disp;
  };
}
```

### ui.js

`resolveName` を渡す箇所は5つ。いずれも `shiftMonthly.nameMap` から作る
（月間シフトCSVが未読み込みなら `null` を渡す）。

1. 「自動配置を実行」内の `parseRookieRows` / `parseSecretRows` / `parseOjtRows`（649〜655行付近）
2. `refreshRuleIndexesFromSecret`（secret.csv の再読み込み）
3. `refreshOjtIndexesFromOjt`（ojt.csv の再読み込み）
4. `refreshRookieIndexesFromRookie`（rookie.csv の再読み込み）
5. **氏名の手入力編集**（1340行付近の編集フォームの保存時）
   … 入力された氏名に `resolveName` を適用する。手で入力した氏名がシフトCSVの表記に
   寄るため、バッジやルールの再判定が効くようになる

### 保存ファイルの読み込み（2422行付近）

過去に保存した配置には、正規化前の氏名が入っている可能性がある。
読み込んだ氏名に `displayName` を適用しておく（`resolveName` までは不要）。

---

## 4. 副作用（意図した挙動変更）

- **secret.csv で「山田太郎」と「山田 太郎」が同じ種別に2行ある場合、これまでは別人として
  両方通っていたが、今後は同一人物の重複として中断する（B-1）。** 望ましい変更。
- ojt.csv の教官重複・OJT対象者の担当重複（B-2 / B-3）も同様に検出されるようになる。
- 氏名の表記ゆれ警告は不要になる（一致してから警告するものが無くなる）。

---

## 5. 動作確認

| 入力 | 期待 |
|---|---|
| shift「山田 太郎」／secret 固定席「山田太郎」 | 固定席が効く |
| shift「山田 太郎」／rookie「山田　太郎」（全角） | 新人固定席が効く |
| shift「山田 太郎」／ojt 教官名「 山田 太郎 」（前後空白） | 教官として処理される |
| shift の氏名セルに改行が入っている | 座席カードが崩れず「山田 太郎」と表示される |
| secret 固定席に「山田太郎」と「山田 太郎」の2行 | 重複として中断する |
| secret に、シフトCSVに存在しない氏名（休職者） | 従来どおり静かに効かない。メッセージには書いたまま出る |
| 月間シフトCSVを読む前に secret.csv を読んだ場合 | 落ちない（`resolveName` が `null` で `displayName` のみ動く） |

---

## 6. `NameMap` / `NameSet` 案を採用しない理由

- 照合箇所が約60か所あり、すべてを置き換える必要がある。差分が大きく、
  置き換え漏れが1か所あると「一部のルールだけ効かない」という原因の分かりにくい
  不具合になる。
- 表示名を別に持つ必要が出るため、画面・印刷・保存ファイル・pkey のどこで
  どちらを使うかという判断が全体に広がる。
- 入口で書き換える方式なら、変更は csv.js の4関数と ui.js の6か所で完結し、
  下流は「氏名は月間シフトCSVの表記で揃っている」という1つの前提だけを持てばよい。
