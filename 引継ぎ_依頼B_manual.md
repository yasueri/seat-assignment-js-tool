# 座席配置ツール 引継ぎ資料（ver4.18 時点）— 依頼B：manual.html の作り直し＋文書突合せ

新しいチャットの冒頭に、この資料の **§1〜§4 と「依頼B」** を貼り付けてください。
あわせて `manual.html` / `index.html` / `algorithm.js` / `ui.js` / `csv.js` を添付してください。

---

## §0. このチャットの位置づけ（前チャットからの引き継ぎ）

前チャットで **依頼A（コードのリファクタリング）は完了**し、コードは **ver4.18** になりました。

- 実行コードの変更は **既定値2箇所のみ**（`poolCap` 60→100、`timeBudgetMs` 10000→5000）。
  どちらも ui.js が常に明示的な値を渡すため、**実運用の挙動は ver4.17 と同一**です。
- 残りはすべてコメント・未使用CSSの整理で、`ui.js` と `csv.js` は**コメント以外は原本と完全同一**であることを機械照合で確認済みです。

**したがって、このチャットでは ver4.18 のコードを「正」として扱ってください。**
コードを直す必要はありません。直すのは `manual.html` だけです。

### 前チャットで判明した重要な経緯（同じ轍を踏まないために）

依頼Aの診断を3巡行い、**巡を重ねるたびに新しい誤りが出てきました**。原因は診断方法にあり、
1〜2巡目は「怪しいと思った用語をgrepする」方式だったため、**思いついたものしか見つからない**構造でした。
3巡目に「全コメントを機械抽出し、主張をコードの実行結果と照合する」網羅方式へ切り替えて、ようやく収束しました。

**manual.html の突合せでも同じ方針を採ってください。**
「気になった箇所を確認する」のではなく、**マニュアル中の検証可能な主張をすべて洗い出し、
1つずつコードと突き合わせる**進め方をお願いします。3巡目で実際に効果があった検証軸は §4 に列挙しています。

---

## §1. 現状

### ファイル構成（readme.txt のとおり。これが正しい構成です）

```
任意のフォルダ/
├─ index.html        ... 本体
├─ manual.html       ... マニュアル（★今回の対象）
├─ css/
│   └─ style.css
├─ js/
│   ├─ csv.js        ... CSVの読み込み・解析・入力チェック
│   ├─ algorithm.js  ... 自動配置ロジック
│   └─ ui.js         ... 画面制御
└─ readme.txt
```

| ファイル | 行数 | 役割 |
|---|---|---|
| `index.html` | 298 | 画面の骨組み、折りたたみ説明（トグル10個） |
| `css/style.css` | 743 | 全スタイル |
| `js/algorithm.js` | 1,877 | 座席割り当てロジック（配置順・全探索・採点） |
| `js/ui.js` | 2,061 | 画面描画、CSV適用、保存/読込、印刷、候補パネル |
| `js/csv.js` | 430 | CSVパース |
| `manual.html` | 588行＋画像11枚 | 図解マニュアル（★今回の対象） |

**注意**: `manual.html` は `index.html` と**同じ階層**です（`handouts/` ではありません）。
index.html からのリンクは `manual.html#アンカー` 形式で、**現状のリンクは正しく、修正不要**です。

### 環境制約（絶対条件）

- 完全オフライン、ブラウザで `index.html` を開くだけで動作する
- サーバー・ビルドツール・Node.js ランタイム・外部CDN は一切使えない
- ES Modules 不可（`file://` の CORS 制限）。`window` 名前空間の共有方式、素のJSのみ
- 利用可能なのはブラウザ・Office・VS Code のみ
- **画面のスクリーンショットはブラウザ実行環境がないと撮影できない**ため、
  新規スクショが必要な場合は利用者側で撮影して渡す（base64で埋め込む）

### manual.html の構成（章立て）

| アンカー | 見出し | 状態 |
|---|---|---|
| `sec-3min` | 1. 3分で分かる使い方 | **完成済み。1バイトも変更禁止（画像含む）** |
| `sec-seatmap` | 2. 画面の見かた：座席マップ | 未完成 ★対象 |
| `sec-badge` | 3. 画面の見かた：バッジ | 未完成 ★対象 |
| `sec-priority` | 4. 自動配置の順番（優先順位） | 未完成 ★対象 |
| `sec-buttons` | 5. ボタンと注意点 | 未完成 ★対象 |
| `sec-trouble` | 6. 困ったときは | 未完成 ★対象 |
| `sec-csv-table` | 7. CSVファイル早見表 | 未完成 ★対象 |
| `sec-csv-write` | 8. ファイル別の書き方 | 未完成 ★対象 |

### index.html のトグル（10個）と役割分担

トグルは**要点メモ＋マニュアルへのリンク**、manual.html が**正典（詳細はこちら）**という分担です。

1. CSVの形式について → `manual.html#sec-csv-write`
2. 自動配置の処理順序について → `manual.html#sec-priority`
3. 座席カードの見かたについて → `manual.html#sec-badge`
4. 操作方法について（日勤）→ `manual.html#sec-buttons`
5. 候補欄（別案の切り替え）について → `manual.html#sec-buttons`
6. 夜勤パネルの見かたについて → `manual.html#sec-priority`
7. 操作方法について（夜勤。日勤側を参照する案内のみ）
8. 手動変更後 配置違反チェックについて → `manual.html#sec-buttons`
9. 印刷について → `manual.html#sec-buttons`
10. 配置の保存・読み込みについて → `manual.html#sec-buttons`

**index.html のトグル本文は ver4.16/4.17 の内容が反映済みで、前チャットで全文を確認し
誤りゼロでした。**マニュアルとコードが食い違った場合、index.html の記述が参考になります。

**気づいた点**: index.html がリンクしているアンカーは `sec-csv-write` / `sec-priority` /
`sec-badge` / `sec-buttons` の**4つだけ**で、`sec-seatmap` / `sec-trouble` / `sec-csv-table`
へのリンクはありません。目次からは辿れますが、必要ならリンクの追加もご検討ください。

---

## §2. 確定仕様（ver4.16 / 4.17 / 4.18。変更しないこと）

**以下はすべて前チャットで実際にコードを実行して裏を取った内容です。**
マニュアルはこの内容と一致させてください。

### 配置ルールの優先順位

```
優先フラグ → 新人固定席 → 教官・OJT → 固定席 → 要サポート
→ 隣接禁止（全探索） → 禁止席だけの対象者 → その他スタッフ（出勤時刻順）
```

- 優先フラグが同数値のときの並びは「新人 → 教官・OJT → 固定席 → 要サポート → その他」→ 出勤時刻順
- 座席9番は自動配置の対象外（明示指定時のみ例外配置し、メッセージを出す）。手動配置は常に可能

### 隣接禁止の繰り上げ再探索（実行検証済み）

解が1通りも見つからない場合、隣接禁止を1段階ずつ前へ動かして再探索する。

| 段階 | 配置順 |
|---|---|
| 段階0（既定） | 優先フラグ → 新人固定席 → 教官・OJT → 固定席 → 要サポート → 隣接禁止 → 禁止席のみ → その他 |
| 段階1 | 優先フラグ → 新人固定席 → 教官・OJT → 固定席 → 隣接禁止 → 要サポート → 禁止席のみ → その他 |
| 段階2（上限） | 優先フラグ → 新人固定席 → 教官・OJT → 隣接禁止 → 固定席 → 要サポート → 禁止席のみ → その他 |

- 優先フラグ・新人固定席・教官OJT より前へは動かさない（上限＝段階2）
- 全段階失敗時は、**段階2の優先順位のまま**貪欲法にフォールバックする

### 座席の定義（実行検証済み）

- 全15席。4列×4行から右下1席を除く。列ごとに上から連番（列1=1〜4、列2=5〜8、列3=9〜12、列4=13〜15）
- **隣接禁止の「隣」は全方向**（上下・左右・斜め）
  - 座席1の隣 → **2・5・6**
  - 座席6の隣 → **1・2・3・5・7・9・10・11**
- **夜勤の隣接回避だけは同列上下のみ**（ソフトな回避。`COLUMN_ADJACENCY`）

### デフォルト座席（実行検証済み）

- **新人（rookie.csv）**: `5 → 10 → 6 → 11 → 7 → 12 → 8`（上位7名・OPのみ。8人目以降は通常探索）
  - ⚠️ ver4.15 までの「5〜8の4席」は**古い情報**です。10・11・12 を含みます
- **教官1名+OJT1名**: `15 → 12 → 8 → 4 → 3 → 2 → 1 → 他`
- **教官1名+OJT2名**（ペア。全12組）:
  - 主要6組: `(12,8) → (12,15) → (15,14) → (4,3) → (3,2) → (2,1)`
  - その他扱いの隣接席6組: `(12,11) → (8,7) → (11,10) → (7,6) → (6,5) → (14,13)`
  - ojt.csvの対象座席で15を12より先に指定した教官のみ `(15,14)` を先頭に試す
- 教官2名・OJT2名で内訳が「2名担当＋0名担当」なら、**教官1名+OJT1名ずつに自動で組み替える**

### 全探索と候補UI

- 全探索の対象は**隣接禁止対象者と禁止席だけの対象者の両方**
  - 隣接禁止側：最大100件集めて採点・並べ替え、上位99件を候補として切替可能
  - 禁止席側：実行可能解を1つ見つけた時点で確定（採点なし。「一部シャッフル」で別解に切替）
  - どちらも解けない／時間切れのときだけMRV貪欲にフォールバック
- 制限時間は1段階あたり **5秒**（`EXHAUSTIVE_TIME_BUDGET_MS`）
- `EXHAUSTIVE_POOL_CAP = 100` / `EXHAUSTIVE_MAX_SOLUTIONS = 99`（poolCap = 表示上限+1）
- 採点は ①`preferredMiss` → ②`boundaryMatches` → ③`variance` の厳密な**3キーの辞書式**

### 候補パネルのボタンと表示

| ボタン | ツールチップ |
|---|---|
| 次案を表示 | 次の候補を表示します |
| 候補1に戻す | 候補1（最良と判定された案）に戻します |
| 一部シャッフル | 禁止席対象者と条件指定の無いスタッフのみシャッフルします |

候補欄の下に出すラベル（3種）:

- `最良グループ（○件）`
- `同時刻入替が○件多い案`（内部名は `boundaryMatches`）
- `指定席どおりでない方が○名`（内部名は `preferredMiss`）

メッセージ欄の件数表示（3通り）:

- 探索し尽くした → `32通り見つけました`
- 上限で打ち切り → `99通り以上見つけました`
- 時間切れ → `47通り見つけたところで、探索の制限時間（5秒）になりました`

### その他の確定値（実行検証済み）

- **夜勤判定**: 開始時刻が22:00より遅い、または終了時刻が26:00より遅い（対象はOPのみ）
- **遅番判定**: 開始が12:00以降、または（前残業TRUEかつ開始が10:00以降）。対象は役席・GL
- **早番・遅番エリア**: 各2行×3列＝6枠、1枠1名。7人目以降は「あふれ」
- **夜勤の役席・GL**: 1名なら夜勤GL枠。2名以上なら1名が夜勤GL枠、次の1名が座席10、3人目以降は通常配置

### 保存データ

- secret.csv 由来の情報（固定席・禁止席・要サポート・隣禁止・優先フラグ・夜勤GL席）は**保存しない**
- rookie.csv / ojt.csv 由来のバッジ情報は**保存する**（配置の制約を推測させないため例外扱い）
- ojt.csv 未読み込みで復元した場合は、保存データの教官・OJT情報をそのまま維持

### バッジの色

- 「隣禁止」= rose（`--adjacent`）／「禁止席」= amber（`--lock`）。**色で区別する**

---

## §3. 決着済みの論点（蒸し返さないでください）

| 論点 | 結論 | 理由 |
|---|---|---|
| 劣後する候補を非表示にするか | **隠さない**。ラベルで示す | ②③はソフト指標。1点差で選択肢を奪うと「次案を表示」の意味が失われる |
| 枝刈りを強化するか | **しない** | Hall条件の簡易版を試作したがノード84万→32万に減るだけで、複雑さに見合わない |
| 制限時間 | **5秒** | 解が存在するケースは実測で全て0.5秒以内 |
| poolCap | **100**（表示は99通り以上） | 表示上限+1 で「まだ他にもある」を厳密に判定できる |
| スコアの計算方法 | **変更しない** | |
| コードのリファクタリング | **依頼Aで完了（ver4.18）** | backtrack共通化・MRV貪欲共通化・step統合は「リスクに見合わない」として**見送り確定** |
| `placeForbiddenGroup` の別名解消 | **やらない** | jsdomテストが旧名を参照している可能性があり、実害は「名前が2つある」だけ |

---

## §4. manual.html の突合せで使ってほしい検証軸

前チャットの3巡目で**実際に誤りを発見できた**軸です。マニュアルにも同じ観点を当ててください。

1. **数値・順序の主張**（座席順・段階・秒数・件数・人数）
   → §2 の確定値と1つずつ照合。特に **新人固定席の「5〜8のみ」は ver4.15 の古い記述**なので要注意
2. **「例：〜」の具体例**
   → 隣接席の例などは実際に計算して検証（座席1→2・5・6／座席6→1・2・3・5・7・9・10・11）
3. **ボタン名・ラベル名**
   → ver4.17 で複数改名済み。旧名「次の案」「別案を表示」「最良に戻す」「その他だけシャッフル」
      「シャッフル」「境界同席」が残っていないか
4. **「〜予定」「将来〜」の記述**
   → 既に実装済みの機能が「予定」のまま書かれていないか（コード側で実際に1件ありました）
5. **廃止済み仕様の記述**
   → 「同列1つ前の座席をフォールバックに使う」（ver4.16で削除）、
      「禁止席のみは貪欲で配置」（ver4.12で全探索に変更）などが残っていないか
6. **index.html のトグル本文との一致**
   → トグルは誤りゼロを確認済みなので、突き合わせの基準として使える
7. **リンク・アンカーの実在**
   → 目次のリンク先アンカーが本文に実在するか

---

## 依頼B：manual.html の2章以降の作り直し（このチャットの本題）

以下を貼り付けてご依頼ください。

> 添付の `manual.html` の、**「2. 画面の見かた：座席マップ」以降**をゼロベースで作り直してください。
> `index.html` / `algorithm.js` / `ui.js` / `csv.js` も添付するので、実際の挙動と一致させてください。
>
> **前提**
>
> - **「1. 3分で分かる使い方」は完成済みです。1バイトも変更しないでください**（画像を含む）
> - 既存の画像11枚はbase64で埋め込まれています。差し替えの必要がなければそのまま残してください
> - 読み手は、文章を読むのが得意ではないコールセンターのスタッフです
> - コードは ver4.18 が正です。**コード側は直さないでください**
>
> **方針**
>
> - 必要事項は網羅する。ただし**長くなりすぎない**こと
> - 「読めば分かる」ではなく「見れば分かる」を優先（表・図・箇条書き中心）
> - 章ごとに「まず結論、次に詳細」の順
> - `index.html` のトグル（折りたたみ説明）は要点メモ＋本マニュアルへのリンクという役割分担なので、
>   詳細はマニュアル側に置く
>
> **進め方のお願い**
>
> 前チャットで、確認のたびに新しい誤りが出てくる状況がありました。原因は「気になった箇所だけを
> 確認する」進め方にあり、網羅的な照合に切り替えて解決しました。今回も同じにならないよう、
> **書き始める前に、現行 manual.html の2章以降に含まれる検証可能な主張（数値・順序・ボタン名・
> 具体例）を洗い出し、上記コードと突き合わせた結果を一覧で報告してください。**
> そのうえで、章ごとの構成案を提示してから本文の作成に入ってください。

---

## §5. 共通の注意事項

- manual.html の画像はbase64埋め込み。差し替えるときは256色に量子化するとサイズを抑えられる（実績：145KB → 112KB）
- 画面のスクリーンショットはブラウザ実行環境がないと撮影できないため、必要なときは利用者側で撮影して渡す
- 出力時はZIPにまとめない（ファイル名が文字化けするため）
- 座席レイアウトは4列×4行から右下1席を除いた**全15席**
